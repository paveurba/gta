---
phase: 11-server-modularization
plan: 03
type: execute
wave: 1
depends_on:
  - 11-02-PLAN.md
files_modified: []
autonomous: true
requirements:
  - REFACTOR-02
user_setup: []
must_haves:
  truths:
    - Shared type **`server/types/playerSession.ts`** — **`PlayerSession`** used by `index.ts` and registrars.
    - **`commands/handleChatCommand.ts`** exports **`handleChatCommand(deps, player, command, args)`** and **`ChatCommandDeps`**; all `/` chat switch cases move out of `index.ts`.
    - **`events/registerPlayerLifecycleEvents.ts`** — **`playerConnect`**, **`playerDisconnect`**, **`playerDeath`** (including **`HOSPITAL_SPAWNS`**, **`HOSPITAL_FEE`**, **`getNearestHospital`**).
    - **`registerAuthClientEvents`**, **`registerChatClientEvents`**, **`registerPhoneClientEvents`**, **`registerWeaponShopClientEvents`**, **`registerClothingShopClientEvents`**, **`registerCasinoClientEvents`** — each thin `alt.onClient` wiring with typed context; **no RPC name or behavior change**.
    - **`registerVehicleClientEvents` / `registerPropertyClientEvents`** — **`getSession`** return type uses **`PlayerSession`** (remove duplicate session shape types).
    - **`pnpm run compile:ts`** passes.
  artifacts:
    - Update **ROADMAP**, **REQUIREMENTS**, **STATE**, **PROJECT**, **ARCHITECTURE**, **CONCERNS**, and cross-phase docs that still say handlers live only in **`index.ts`** (death, shops, phone).
  key_links:
    - `server/index.ts` — remains orchestration: pool, services, session helpers, parked spawns, **`register*()`** calls
---

<objective>
**REFACTOR-02:** Complete the incremental **`server/index.ts`** split started in **11-01/11-02** — lifecycle, auth UI RPCs, chat + command routing, phone, shops, casino — using the same **registrar + small context** pattern (**KISS / SRP**).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-server-modularization/11-CONTEXT.md
@.planning/phases/11-server-modularization/11-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlayerSession + lifecycle registrar</name>
  <files>src/plugins/gta-mysql-core/server/types/playerSession.ts (new), src/plugins/gta-mysql-core/server/events/registerPlayerLifecycleEvents.ts (new)</files>
  <read_first>index.ts connect/disconnect/death blocks; shop location imports</read_first>
  <action>
1. Extract **`PlayerSession`** interface to **`types/playerSession.ts`**; **`index.ts`** imports it.

2. Move **`playerConnect`**, **`playerDisconnect`**, **`playerDeath`** and hospital constants/helpers into **`registerPlayerLifecycleEvents(ctx)`**; **`index.ts`** calls it with **`spawnPlayerSafe`**, **`savePlayerMoney`**, **`applyCharacterLook`**, maps, services.
  </action>
  <verify>pnpm run compile:ts</verify>
  <acceptance_criteria>
    - Death fee, delay, **`gta:spawn:safe`** (no coords), **`playersInProperty.delete`** unchanged semantically
  </acceptance_criteria>
  <done>Lifecycle module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Auth + shop + phone registrars</name>
  <files>server/events/registerAuthClientEvents.ts, registerChatClientEvents.ts, registerPhoneClientEvents.ts, registerWeaponShopClientEvents.ts, registerClothingShopClientEvents.ts, registerCasinoClientEvents.ts (new)</files>
  <read_first>index.ts auth:*, gta:chat:send, phone:*, weaponshop:*, clothingshop:*, casino:*</read_first>
  <action>
1. Move each **`alt.onClient`** group into **`register*(ctx)`** with minimal context (session lookup, services, **`notifyPlayer`**, **`getMySQLPool`** where needed).

2. **`registerChatClientEvents`** takes **`handleCommand`** callback; **`index.ts`** defines thin wrapper after **`chatCommandDeps`** is built.
  </action>
  <verify>pnpm run compile:ts</verify>
  <acceptance_criteria>
    - **`auth:logout`** still clears **`playersInProperty`**; chat **`/logout`** behavior unchanged vs prior **`index`** (no **`playersInProperty`** clear on chat path if that was prior behavior)
  </acceptance_criteria>
  <done>Client RPC registrars compile.</done>
</task>

<task type="auto">
  <name>Task 3: handleChatCommand module + index wiring</name>
  <files>src/plugins/gta-mysql-core/server/commands/handleChatCommand.ts (new), server/index.ts</files>
  <read_first>Full **`handleCommand`** switch in index</read_first>
  <action>
1. Implement **`handleChatCommand`** with **`ChatCommandDeps`** covering services, maps, **`saveAndApplyAppearance`**, **`clamp`**, **`buildPropertyInteriorEnterPayload`**, **`sendTestEmail`**, etc.

2. **`index.ts`**: build **`chatCommandDeps`** object; **`handleCommand`** delegates to **`handleChatCommand`**; remove inlined switch.

3. Align **`registerVehicleClientEvents` / `registerPropertyClientEvents`** **`getSession`** typing on **`PlayerSession`**.
  </action>
  <verify>pnpm run compile:ts</verify>
  <acceptance_criteria>
    - Grep **`switch (command)`** in **`index.ts`** — none (commands only in **`handleChatCommand.ts`**)
  </acceptance_criteria>
  <done>Command routing extracted.</done>
</task>

<task type="auto">
  <name>Task 4: GSD artifacts (this phase)</name>
  <files>.planning/ROADMAP.md, REQUIREMENTS.md, STATE.md, PROJECT.md, codebase/ARCHITECTURE.md, codebase/CONCERNS.md, phases/11-server-modularization/*, selected cross-phase *.md</files>
  <read_first>11-02-SUMMARY pattern; REFACTOR-01 traceability</read_first>
  <action>
1. Add **REFACTOR-02** to **REQUIREMENTS**; checklist and traceability row for **11-03**.

2. **ROADMAP** phase 11: third plan **11-03** checked; goal text mentions full registrar sweep.

3. **STATE** / **PROJECT** / **11-CONTEXT** — reflect shipped work; **ARCHITECTURE** + **CONCERNS** — shorter **`index.ts`**, new modules.

4. Update **06-DEATH-AND-WORLD**, **06-BLIPS**, **05-*-SERVER**, **03-PHONE** one-liners if they still point only at **`index.ts`** for moved handlers.
  </action>
  <verify>Planning links resolve; requirement IDs consistent</verify>
  <acceptance_criteria>
    - **Nyquist-style** plan **11-03-PLAN** + **11-03-SUMMARY** committed with code or immediately after
  </acceptance_criteria>
  <done>GSD traceability complete.</done>
</task>

</tasks>

<verification_loop>
- Manual spot-check: login, chat **`/help`**, shop **E** purchase, phone, casino RPC, death respawn fee.
- **`grep "alt.onClient"`** in **`index.ts`** — should be **zero** (all registration via **`register*`** calls).
</verification_loop>
