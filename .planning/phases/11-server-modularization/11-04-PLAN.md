---
phase: 11-server-modularization
plan: 04
type: execute
wave: 1
depends_on:
  - 11-03-PLAN.md
files_modified: []
autonomous: true
requirements:
  - REFACTOR-03
user_setup: []
must_haves:
  truths:
    - New module **`server/bootstrap/createGameplayMysqlBundle.ts`** — pool creation, **`runMigrations`**, service construction; **`getMySQLPool`** in **`index.ts`** assigns bundle fields (no behavior change).
    - New module **`server/runtime/createPlayerRuntime.ts`** — **`applyCharacterLook`**, **`clamp`**, **`saveAndApplyAppearance`**, **`completeLogin`**, **`savePlayerMoney`**, **`clearExistingSession`**, **`spawnPlayerSafe`**, **`notifyPlayer`**, **`syncMoneyToClient`**, **`broadcastPropertyUpdate`** via **`createPlayerRuntime(ctx)`**; **`bindCharacterForPlayer`** internal to runtime (used by **`completeLogin`**). Dead **`refreshPlayerMoney`** removed (no call sites).
    - New module **`server/world/spawnStaticParkedVehicles.ts`** — **`PARKED_VEHICLE_SPAWNS`**, **`spawnStaticParkedVehicles()`**; **`index.ts`** calls it at startup only.
    - **`pnpm run compile:ts`** passes.
  artifacts:
    - **REFACTOR-03** in **REQUIREMENTS**; **ROADMAP** / **STATE** / **11-CONTEXT** / **ARCHITECTURE** / **06-BLIPS** updated for new paths.
  key_links:
    - `server/index.ts` — thin orchestration only (~≤230 LOC target)
---

<objective>
**REFACTOR-03:** Remove the last large inline blocks from **`server/index.ts`** — MySQL bootstrap, player/session helpers, static parked vehicle data — using small modules (**KISS**), **before** implementation (GSD-first for this slice).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-server-modularization/11-CONTEXT.md
@.planning/phases/11-server-modularization/11-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: createGameplayMysqlBundle</name>
  <files>src/plugins/gta-mysql-core/server/bootstrap/createGameplayMysqlBundle.ts (new)</files>
  <read_first>index.ts getMySQLPool + service constructors</read_first>
  <action>Move pool config, migrations, and **`new *Service(pool)`** into **`createGameplayMysqlBundle()`** returning a single typed object. **`index.ts`** **`getMySQLPool`** assigns module-level service variables from the bundle.</action>
  <verify>pnpm run compile:ts</verify>
  <done>Bundle module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: createPlayerRuntime</name>
  <files>src/plugins/gta-mysql-core/server/runtime/createPlayerRuntime.ts (new)</files>
  <read_first>index.ts session + character + notify block (lines ~88–300)</read_first>
  <action>Export **`createPlayerRuntime(ctx)`** with service accessors (closures) so helpers work after first **`getMySQLPool`**; **`index.ts`** destructures returned functions.</action>
  <verify>pnpm run compile:ts</verify>
  <done>Runtime helpers extracted.</done>
</task>

<task type="auto">
  <name>Task 3: spawnStaticParkedVehicles</name>
  <files>src/plugins/gta-mysql-core/server/world/spawnStaticParkedVehicles.ts (new)</files>
  <read_first>index.ts PARKED_VEHICLE_SPAWNS + spawn function</read_first>
  <action>Move spawn data + **`spawnStaticParkedVehicles`**; **`index.ts`** imports and invokes at end of file.</action>
  <verify>pnpm run compile:ts</verify>
  <done>World spawn module compiles.</done>
</task>

<task type="auto">
  <name>Task 4: GSD closure</name>
  <files>.planning/REQUIREMENTS.md, ROADMAP.md, STATE.md, phases/11-server-modularization/11-CONTEXT.md, 11-04-SUMMARY.md, codebase/ARCHITECTURE.md, phases/06-world-webview/06-BLIPS-AND-STATIC.md</files>
  <read_first>11-03-SUMMARY frontmatter</read_first>
  <action>Add **REFACTOR-03**, plan **11-04** checklist, **STATE** plan count **5**, summary file with verification notes.</action>
  <verify>Links and requirement IDs consistent</verify>
  <done>Traceability complete.</done>
</task>

</tasks>

<verification_loop>
- **`wc -l`** **`server/index.ts`** — materially smaller than pre-phase (~423 LOC → ~220).
- Smoke: **`init()`** still calls **`getMySQLPool`**; parked spawn still runs after **`init()`**.
</verification_loop>
