---
phase: 11-server-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - REFACTOR-01
user_setup: []
must_haves:
  truths:
    - New module under `src/plugins/gta-mysql-core/server/` (e.g. `events/registerVehicleClientEvents.ts`) exports **`registerVehicleClientEvents(ctx)`**.
    - `ctx` is a **typed** object with only dependencies vehicle handlers use: session lookup, `VehicleService`, `PropertyService` (garage flows), `syncMoneyToClient`, `notifyPlayer`.
    - All **`vehicle:*`** `alt.onClient` listeners from `index.ts` (catalog through `spawnFromGarage`) are registered inside that function; **`index.ts` calls it once** after services exist.
    - `pnpm run compile:ts` passes; behavior unchanged (smoke: buy/spawn/store/garage).
  artifacts:
    - Optional one-line in `.planning/codebase/ARCHITECTURE.md` or `CONCERNS.md` pointing at `events/` pattern (only if those files already describe server layout).
  key_links:
    - `server/index.ts` — block between `// VEHICLE EVENTS` and `// COMMAND HANDLER`
    - `VehicleService.ts`, `PropertyService.ts`
---

<objective>
Cut ~160 LOC of vehicle RPC wiring from `server/index.ts` into a dedicated registrar to satisfy **REFACTOR-01** first slice (**single responsibility**).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-server-modularization/11-CONTEXT.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define VehicleHandlersContext + registerVehicleClientEvents</name>
  <files>src/plugins/gta-mysql-core/server/events/registerVehicleClientEvents.ts (new)</files>
  <read_first>server/index.ts vehicle block; Auth session type usage (playerSessions)</read_first>
  <action>
Create **`events/registerVehicleClientEvents.ts`**:

1. Export a **narrow** context type (e.g. `getSession(playerId)`, `vehicleService`, `propertyService`, `syncMoneyToClient`, `notifyPlayer` — match actual usage in the vehicle block).

2. Move **verbatim** handler bodies from `index.ts` into this file; **`registerVehicleClientEvents(ctx)`** contains all `alt.onClient('vehicle:...', ...)` registrations.

3. Use existing imports (`alt-server`, services types). Do **not** change game logic.
  </action>
  <verify>pnpm run compile:ts</verify>
  <acceptance_criteria>
    - No duplicate `alt.onClient` for the same vehicle event name
  </acceptance_criteria>
  <done>Vehicle module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire from index.ts</name>
  <files>src/plugins/gta-mysql-core/server/index.ts</files>
  <read_first>Where mysql pool and services are initialized</read_first>
  <action>
1. Import `registerVehicleClientEvents`.

2. Remove in-file vehicle `alt.onClient` block; call `registerVehicleClientEvents({ ... })` once after `playerSessions` / services / helpers (`notifyPlayer`, `syncMoneyToClient`) are in scope.

3. Pass **the same** service instances index already uses.
  </action>
  <verify>pnpm run compile:ts</verify>
  <acceptance_criteria>
    - `index.ts` line count reduced; vehicle section comment can move above the register call
  </acceptance_criteria>
  <done>Wiring complete.</done>
</task>

</tasks>

<verification_loop>
- Grep `vehicle:` in `index.ts` — should be none inside handlers (only string in comments or register call path ok).
- Manual: dealership buy + garage spawn still work in dev server if available.
</verification_loop>
