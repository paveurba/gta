---
phase: 11-server-modularization
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01-PLAN.md
files_modified: []
autonomous: true
requirements:
  - REFACTOR-01
user_setup: []
must_haves:
  truths:
    - New module **`events/registerPropertyClientEvents.ts`** exports **`registerPropertyClientEvents(ctx)`**.
    - Context includes: session lookup, `PropertyService`, `buildPropertyInteriorEnterPayload`, **`playersInProperty`** `Map`, `syncMoneyToClient`, **`broadcastPropertyUpdate`** (buy/sell success paths).
    - All **`property:*`** `alt.onClient` listeners (`getList` through `exit`) move out of `index.ts`.
    - `pnpm run compile:ts` passes.
  artifacts:
    - Update `04-PROPERTIES-SERVER.md` only if RPC “location” doc says handlers live only in `index.ts` — adjust to “`registerPropertyClientEvents` + index bootstrap”.
  key_links:
    - `server/index.ts` — property block after phone events
---

<objective>
Second **REFACTOR-01** slice: isolate property client RPC wiring the same way as vehicles (**DRY pattern** with 11-01).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-server-modularization/11-CONTEXT.md
@.planning/phases/11-server-modularization/11-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: registerPropertyClientEvents module</name>
  <files>src/plugins/gta-mysql-core/server/events/registerPropertyClientEvents.ts (new)</files>
  <read_first>index.ts property handlers; playersInProperty Map type</read_first>
  <action>
1. Create **`PropertyHandlersContext`** with only fields property handlers need (including `playersInProperty: Map<number, number>` and `broadcastPropertyUpdate: () => Promise<void>`).

2. Move `property:getList`, `requestList`, `buy`, `sell`, `enter`, `exit` handlers into **`registerPropertyClientEvents(ctx)`**.

3. Keep **`buildPropertyInteriorEnterPayload`** import from `PropertyService` path used elsewhere.
  </action>
  <verify>pnpm run compile:ts</verify>
  <acceptance_criteria>
    - Enter/exit still update `playersInProperty` exactly as before
  </acceptance_criteria>
  <done>Property module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: index.ts wiring + doc touch</name>
  <files>src/plugins/gta-mysql-core/server/index.ts, .planning/phases/04-properties-vehicles/04-PROPERTIES-SERVER.md</files>
  <read_first>Call order: property register should run when broadcastPropertyUpdate and maps exist</read_first>
  <action>
1. Remove property `alt.onClient` block from `index.ts`; call `registerPropertyClientEvents(ctx)` near vehicle registrar.

2. If `04-PROPERTIES-SERVER.md` claims all handlers are inline in `index.ts`, add one sentence pointing to `server/events/registerPropertyClientEvents.ts`.
  </action>
  <verify>pnpm run compile:ts</verify>
  <acceptance_criteria>
    - Grep `property:` `onClient` in index.ts — none remain for these six events
  </acceptance_criteria>
  <done>Phase 11 property slice complete.</done>
</task>

</tasks>

<verification_loop>
- Buy/sell still call `broadcastPropertyUpdate` on success.
- Chat `enter`/`exit` in `handleCommand` unchanged in this plan (still in index) — document if intentional.
</verification_loop>
