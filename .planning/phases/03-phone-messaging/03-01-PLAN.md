---
phase: 03-phone-messaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - PHON-01
  - PHON-02
user_setup: []
must_haves:
  truths:
    - Chat `/contact`, `/contacts`, `/sms` map to `PhoneService` with session-gated `session.oderId`.
    - Server `alt.onClient('phone:getData'|'phone:addContact'|'phone:deleteContact'|'phone:sendMessage')` call into `PhoneService` and emit `phone:data` / notify as documented.
  artifacts:
    - `.planning/phases/03-phone-messaging/03-PHONE-SERVER.md` lists every handler and SQL table touched.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/PhoneService.ts`
    - `src/plugins/gta-mysql-core/server/index.ts` (Phone events + `handleCommand` cases)
---

<objective>
Verify and document **server-side phone persistence and RPC**: `PhoneService`, `phone:*` client→server events, chat commands `/contact`, `/contacts`, `/sms`, and MySQL tables `phone_contacts` / `phone_messages`.

Purpose: Satisfy **PHON-01** and **PHON-02**.

Output: `03-PHONE-SERVER.md`; README Phone section clarifications if drift (e.g. `/sms` uses **database player id**, not alt:V transient id).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/PhoneService.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author server reference doc</name>
  <files>.planning/phases/03-phone-messaging/03-PHONE-SERVER.md</files>
  <read_first>src/plugins/gta-mysql-core/server/services/PhoneService.ts, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
Create **new file** `.planning/phases/03-phone-messaging/03-PHONE-SERVER.md` with sections:

1. **Tables:** `phone_contacts`, `phone_messages` — which methods read/write each.

2. **Client→server events:** For each of `phone:getData`, `phone:addContact`, `phone:deleteContact`, `phone:sendMessage` — file path, session guard (`playerSessions.get`), `PhoneService` method called, and client response (`phone:data` emit where applicable).

3. **Chat commands:** Document `contact`, `contacts`, `sms` branches in `handleCommand`: usage strings exactly as in code (`Usage: /contact <name> <number>`, etc.).

4. **Receiver notification:** When `sendMessage` succeeds and receiver is online, document `alt.emitClient(receiverPlayer, 'phone:newMessage', messageData)` and how `receiverPlayer` is resolved (`playerId` meta vs `players.id`).

5. **ID semantics:** State explicitly that `/sms <playerId>` and `phone:sendMessage` use **MySQL `players.id`** (same as `session.oderId` / `player.setMeta('playerId')`), not alt:V `player.id`.
  </action>
  <verify>test -f .planning/phases/03-phone-messaging/03-PHONE-SERVER.md</verify>
  <acceptance_criteria>
    - `03-PHONE-SERVER.md` contains the substring `phone:getData`
    - Same file contains `phone_messages` and `phone_contacts`
    - Same file contains `/sms` or `sms` case documentation
  </acceptance_criteria>
  <done>Server phone surface fully mapped.</done>
</task>

<task type="auto">
  <name>Task 2: Align README Phone commands with code</name>
  <files>README.md</files>
  <read_first>README.md, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
1. Under **Phone** in `README.md`, ensure `/sms` description states the target is the **persisted player id** (`players.id` / in-game account id), not the alt:V connection id.

2. Add one line: contacts added via `/contact` store `contact_number` as provided; messaging targets **numeric player ids** (see `PhoneService.sendMessage`).

3. If README implies Vue **webview** for the phone, **remove or correct** that implication for the phone feature — the default phone UI in this repo is the **native client overlay** in `client/index.ts` (execution plan 03-02 will detail). Minimum fix: README must not claim phone is Vue webview unless `webview/` actually implements phone (it does not).
  </action>
  <verify>grep -n "### Phone" README.md</verify>
  <acceptance_criteria>
    - `README.md` Phone section contains either `players.id` or `player id` (case-insensitive) clarifying SMS target
    - `README.md` does not contain the phrase `Vue` immediately describing the in-game phone menu (if Vue is mentioned only for other UI, that is OK)
  </acceptance_criteria>
  <done>Operator docs match server contract.</done>
</task>

</tasks>

<verification>
- [ ] Server doc complete
- [ ] README Phone section accurate
</verification>

<success_criteria>

- PHON-01 / PHON-02 traceable to concrete handlers and SQL
- No false claim that `/sms` uses alt:V `player.id`

</success_criteria>
