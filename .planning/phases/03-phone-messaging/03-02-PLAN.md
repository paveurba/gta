---
phase: 03-phone-messaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/plugins/gta-mysql-core/server/index.ts
autonomous: true
requirements:
  - PHON-03
user_setup: []
must_haves:
  truths:
    - **M** key (code `77`) toggles phone when logged in; documented consistently in README and server welcome text.
    - Client `openPhone` emits `phone:getData`; `phone:data` / `phone:newMessage` update local phone state.
  artifacts:
    - `.planning/phases/03-phone-messaging/03-PHONE-CLIENT.md` describes native overlay flow (not Vue webview).
  key_links:
    - `src/plugins/gta-mysql-core/client/index.ts` (`openPhone`, `handlePhoneKey`, key `77`)
---

<objective>
Document the **native alt:V client phone UI** (drawn in `client/index.ts`), **hotkey**, and **server sync events**; fix any **wrong hotkey hint** in server welcome vs README.

Purpose: Satisfy **PHON-03**.

Output: `03-PHONE-CLIENT.md`; README Hotkeys unchanged unless correction needed; `playerConnect` welcome string uses **M** not **P** for phone if currently wrong.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@README.md
@src/plugins/gta-mysql-core/client/index.ts
@src/plugins/gta-mysql-core/server/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write client phone UI reference</name>
  <files>.planning/phases/03-phone-messaging/03-PHONE-CLIENT.md</files>
  <read_first>src/plugins/gta-mysql-core/client/index.ts</read_first>
  <action>
Create **new file** `.planning/phases/03-phone-messaging/03-PHONE-CLIENT.md` covering:

1. **Hotkey:** Key code **77** = **M** — when `isLoggedIn`, toggles `openPhone` / `closePhone`. Note `authOpen` / `chatOpen` / `propertyInteractionOpen` guards that block opening.

2. **Flow:** `openPhone()` → `alt.emitServer('phone:getData')` → server responds `phone:data` → `onServer('phone:data')` fills `phoneContacts`, `phoneMessages`, `phoneUnread`.

3. **Outbound actions:** Which keys call `alt.emitServer('phone:addContact', ...)`, `phone:sendMessage`, etc. (summarize from `handlePhoneKey` / related functions — file line ranges OK).

4. **Architecture note:** Phone UI is **native client drawing** (this file), **not** `webview/` Vue pages. Cross-reference `webview/` only if shared HUD also shows phone (if not, say "no Vue phone page").

5. **Incoming SMS:** `onServer('phone:newMessage')` prepends message and increments unread.
  </action>
  <verify>grep -n "openPhone" src/plugins/gta-mysql-core/client/index.ts | head -3</verify>
  <acceptance_criteria>
    - `03-PHONE-CLIENT.md` contains `phone:getData` and `phone:data`
    - Same file states key **77** or the letter **M** for phone toggle
    - Same file explicitly says phone UI is **native** client or **not** Vue webview
  </acceptance_criteria>
  <done>Client phone behavior documented.</done>
</task>

<task type="auto">
  <name>Task 2: Fix welcome hotkey and roadmap wording</name>
  <files>src/plugins/gta-mysql-core/server/index.ts, .planning/ROADMAP.md</files>
  <read_first>src/plugins/gta-mysql-core/server/index.ts, README.md, .planning/ROADMAP.md</read_first>
  <action>
1. In `playerConnect` notify string in `src/plugins/gta-mysql-core/server/index.ts`, if the text says **P** for phone, change it to **M** to match `README.md` Hotkeys and client key handler.

2. In `.planning/ROADMAP.md` **Phase 3** goal line, if it says **webview phone UI**, change to **native phone UI (M key) with server-backed contacts/messages** or equivalent accurate wording (keep requirement IDs unchanged).
  </action>
  <verify>grep -n "playerConnect" src/plugins/gta-mysql-core/server/index.ts</verify>
  <acceptance_criteria>
    - `server/index.ts` `playerConnect` notification contains `Press M` or `press M` for phone (case-insensitive), not `Press P for phone`
    - `.planning/ROADMAP.md` Phase 3 overview or goal does not claim phone is implemented in **Vue webview** unless true
  </acceptance_criteria>
  <done>Hints match implementation.</done>
</task>

</tasks>

<verification>
- [ ] Client doc exists
- [ ] Welcome string and ROADMAP wording fixed if needed
</verification>

<success_criteria>

- PHON-03 satisfied with accurate hotkey + UI architecture
- Operators are not told wrong key on connect

</success_criteria>
