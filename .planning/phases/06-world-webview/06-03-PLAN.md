---
phase: 06-world-webview
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - WEBV-01
user_setup: []
must_haves:
  truths:
    - Vue app lives under `webview/`; Docker / `pnpm refresh` build path compiles webview (see root `package.json` / `docker-compose` / compile scripts — cite actual commands from repo).
    - Rebar registers webview pages via `webview/pages/plugins.ts` → `PLUGIN_IMPORTS` (may be empty); core gameplay UIs documented as native in earlier phases must stay distinguished from Vue pages.
  artifacts:
    - `.planning/phases/06-world-webview/06-WEBVIEW.md` describes build pipeline, output location consumed by alt:V resource, and smoke checklist (game starts, no webview load errors if pages exist).
  key_links:
    - `webview/package.json`, `webview/vite.config.ts`
    - `src/main/shared/webview/index.ts`, `src/main/client/webview/index.ts`
    - Root `package.json` / `scripts/` for `refresh` or Docker build
---

<objective>
Document **Vue webview build and integration** with Rebar/alt:V and clarify **which in-game flows use webview vs native client** so **WEBV-01** is verifiable.

Purpose: Satisfy **WEBV-01**.

Output: `06-WEBVIEW.md`; README **Tech stack** or **Quick start** notes how to build/load webview; no false claim that phone/properties are Vue if they remain native.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@webview/package.json
@package.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Webview integration reference</name>
  <files>.planning/phases/06-world-webview/06-WEBVIEW.md</files>
  <read_first>root package.json scripts, webview vite config, PLUGIN_IMPORTS</read_first>
  <action>
Create **`.planning/phases/06-world-webview/06-WEBVIEW.md`**:

1. **Build:** Command(s) to produce webview assets (`pnpm` in `webview/`, or monorepo script); where output is written (dist path) and how Docker image picks it up.

2. **Registration:** `PLUGIN_IMPORTS` / `webview/pages/plugins.ts` — current state (empty vs populated); how future plugins register pages.

3. **Runtime:** High-level Rebar webview events (`SystemEvents.player.webview` from shared types) — one paragraph, no full API dump.

4. **Scope:** Explicit list: Auth UI, phone, property menus — **native** (`gta-mysql-core/client`); Vue webview — **when** used (e.g. Rebar default pages only if none registered).

5. **Smoke checklist:** After `pnpm refresh` or documented build: client connects; note checking alt:V F8 / logs for webview errors (concrete steps for operators).
  </action>
  <verify>test -f .planning/phases/06-world-webview/06-WEBVIEW.md</verify>
  <acceptance_criteria>
    - File names `webview/` directory
    - File mentions `pnpm refresh` or the actual repo compile path
    - File states whether `PLUGIN_IMPORTS` is empty and what that implies
  </acceptance_criteria>
  <done>Webview path documented.</done>
</task>

<task type="auto">
  <name>Task 2: README webview build note</name>
  <files>README.md</files>
  <read_first>README Quick Start, 06-WEBVIEW.md</read_first>
  <action>
Add a short **Webview (Vue)** subsection or bullets under Tech Stack / Quick Start:

- Webview is built as part of **`pnpm refresh`** / Docker image (match repo).

- Primary gameplay HUDs may still be **native**; webview is the Rebar/Vue pipeline for registered pages.

Do not state that phone or shops run in Vue if code shows otherwise.
  </action>
  <verify>grep -n "webview\|Vue" README.md</verify>
  <acceptance_criteria>
    - README mentions Vue/webview build in relation to `pnpm refresh` or Docker
    - README does not contradict native phone/properties docs
  </acceptance_criteria>
  <done>Operators know how webview fits the build.</done>
</task>

</tasks>

<verification>
- [ ] Webview doc exists
- [ ] README updated
</verification>

<success_criteria>

- WEBV-01 has a verification story

</success_criteria>
