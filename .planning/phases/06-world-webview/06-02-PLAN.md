---
phase: 06-world-webview
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - WORLD-03
  - WORLD-04
user_setup: []
must_haves:
  truths:
    - `playerDeath` handler uses `getNearestHospital`, **5000 ms** delay, `HOSPITAL_FEE` constant, `savePlayerMoney` when funds suffice, then `applyCharacterLook` + `loadWeaponsToPlayer` for logged-in players.
    - Client runs `disableAmbientPopulation` every tick and `disablePopulationOnce` on `connectionComplete` (budget + dispatch flags).
  artifacts:
    - `.planning/phases/06-world-webview/06-DEATH-AND-WORLD.md` quotes delay ms, fee, hospital table, and population native calls by name.
  key_links:
    - `src/plugins/gta-mysql-core/server/index.ts` (`playerDeath`, `HOSPITAL_SPAWNS`, `HOSPITAL_FEE`)
    - `src/plugins/gta-mysql-core/client/index.ts` (`disableAmbientPopulation`, `disablePopulationOnce`, `alt.everyTick`)
---

<objective>
Document **death → hospital respawn** (fee, timing, persistence side effects) and **ambient population suppression** for multiplayer-clean world.

Purpose: Satisfy **WORLD-03**, **WORLD-04**.

Output: `06-DEATH-AND-WORLD.md`; README **Features** / **Death** wording aligned with `HOSPITAL_FEE` and delay literals in code.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/index.ts
@src/plugins/gta-mysql-core/client/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Death/respawn and population reference</name>
  <files>.planning/phases/06-world-webview/06-DEATH-AND-WORLD.md</files>
  <read_first>server `playerDeath` block; client population functions and `everyTick`</read_first>
  <action>
Create **`.planning/phases/06-world-webview/06-DEATH-AND-WORLD.md`**:

1. **Death flow:** Notify message; `alt.setTimeout(..., 5000)`; spawn position/heading from nearest `HOSPITAL_SPAWNS` row; health/armour reset.

2. **Fee:** `HOSPITAL_FEE` value; branch when `session.money < fee`; MySQL write path (`savePlayerMoney`).

3. **Post-respawn:** `gta:spawn:safe` rationale (comment in server about client ground probe); `playersInProperty` clear; appearance + weapons restore.

4. **Population:** List natives called in `disableAmbientPopulation` (per-frame) and `disablePopulationOnce` (once on connect); note `connectionComplete` ordering with IPL load.

5. **Client death HUD:** If `playerDeath` on client only dims screen / hospital fee text, cross-reference line numbers briefly.
  </action>
  <verify>grep -n "HOSPITAL_FEE\|5000" src/plugins/gta-mysql-core/server/index.ts</verify>
  <acceptance_criteria>
    - File contains `5000` or “5 second”
    - File contains `HOSPITAL_FEE` or `$500`
    - File contains `disableAmbientPopulation` or `setPedDensityMultiplierThisFrame`
  </acceptance_criteria>
  <done>Death and cleanliness documented.</done>
</task>

<task type="auto">
  <name>Task 2: README death/respawn accuracy</name>
  <files>README.md</files>
  <read_first>README Features (death bullet), 06-DEATH-AND-WORLD.md draft values</read_first>
  <action>
Edit README so the **death/respawn** bullet states the **same fee and delay** as `index.ts` (`HOSPITAL_FEE`, timeout ms / seconds).

Ensure **no AI pedestrians/traffic** (or equivalent) matches **WORLD-04** implementation (every-frame + connect-time disables).
  </action>
  <verify>grep -n "500\|5 second\|pedestrian\|traffic\|AI" README.md</verify>
  <acceptance_criteria>
    - README mentions hospital fee amount matching `HOSPITAL_FEE`
    - README mentions ~5s delay or same numeric delay as code
  </acceptance_criteria>
  <done>Operator-facing text matches server.</done>
</task>

</tasks>

<verification>
- [ ] Doc exists
- [ ] README death section verified
</verification>

<success_criteria>

- WORLD-03 and WORLD-04 evidenced end-to-end

</success_criteria>
