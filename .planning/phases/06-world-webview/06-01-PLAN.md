---
phase: 06-world-webview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - WORLD-01
  - WORLD-02
user_setup: []
must_haves:
  truths:
    - Client `recreateWorldBlips()` (or equivalent) creates blips for weapon shops, clothing shops, casinos, properties (for-sale / owned tint), hospitals, dealerships from server + local data.
    - Server `spawnStaticParkedVehicles()` runs at plugin load and fills `PARKED_VEHICLE_SPAWNS` into `alt.Vehicle` instances (engine off, unlocked).
  artifacts:
    - `.planning/phases/06-world-webview/06-BLIPS-AND-STATIC.md` lists blip categories, sprite IDs, data sources (`gta:locations:update`, `property:list`, `HOSPITALS`), and static spawn pipeline.
  key_links:
    - `src/plugins/gta-mysql-core/client/index.ts` (blip creation, `BLIP_SPRITES`, `HOSPITALS`)
    - `src/plugins/gta-mysql-core/server/index.ts` (`gta:locations:update`, `PARKED_VEHICLE_SPAWNS`, `spawnStaticParkedVehicles`)
---

<objective>
Document **map blips** for shops, properties, hospitals, casino, dealerships and **server-side static parked vehicles** so operators can verify **WORLD-01** and **WORLD-02** against code.

Purpose: Satisfy **WORLD-01**, **WORLD-02**.

Output: `06-BLIPS-AND-STATIC.md`; README **Map Locations** / world features aligned with counts and any coord drift (e.g. hospital blip vs `HOSPITAL_SPAWNS` street points).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/client/index.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blips and static vehicles reference</name>
  <files>.planning/phases/06-world-webview/06-BLIPS-AND-STATIC.md</files>
  <read_first>client/index.ts (blips, HOSPITALS), server/index.ts (locations emit, PARKED_VEHICLE_SPAWNS)</read_first>
  <action>
Create **`.planning/phases/06-world-webview/06-BLIPS-AND-STATIC.md`**:

1. **Blip types:** For each category (weapon, clothing, casino, property for-sale/owned, hospital, dealership), document GTA native sprite id, colour, short-range flag, and **data source** (server array vs client constant vs `property:list`).

2. **Server → client:** When `gta:locations:update` fires; which arrays populate blips.

3. **Hospitals:** Compare client `HOSPITALS` coords to server `HOSPITAL_SPAWNS` (respawn targets differ in z/xy for Pillbox — document both purposes: **map icon** vs **spawn**).

4. **Static vehicles:** `PARKED_VEHICLE_SPAWNS` structure; `spawnStaticParkedVehicles` at end of `index.ts`; count of entries; failure logging per model.

5. **Property blips:** Green/blue/gray semantics tied to `owner_player_id` and local player id.
  </action>
  <verify>test -f .planning/phases/06-world-webview/06-BLIPS-AND-STATIC.md</verify>
  <acceptance_criteria>
    - File contains `BLIP_SPRITES` or sprite numbers
    - File contains `PARKED_VEHICLE_SPAWNS` or `spawnStaticParkedVehicles`
    - File mentions `gta:locations:update` or `property:list`
  </acceptance_criteria>
  <done>World presentation documented.</done>
</task>

<task type="auto">
  <name>Task 2: README world markers and static count</name>
  <files>README.md</files>
  <read_first>README.md Features + Map Locations, server PARKED array count</read_first>
  <action>
- Ensure README **static parked vehicles** claim matches **`PARKED_VEHICLE_SPAWNS` length** (update “45+” to exact count if different).

- If hospital **blip** positions intentionally differ from **respawn** coords, add one sentence under Hospitals so operators are not confused.

- Confirm README lists all major blip categories **WORLD-01** expects (shops, properties, hospitals, casino, dealerships) — add missing category only if absent.
  </action>
  <verify>grep -n "Static\|blip\|parked" README.md</verify>
  <acceptance_criteria>
    - README static vehicle count or range matches source array size
    - README mentions blips for core POI categories or defers to `06-BLIPS-AND-STATIC.md`
  </acceptance_criteria>
  <done>README matches blips and static spawns.</done>
</task>

</tasks>

<verification>
- [ ] Reference doc exists
- [ ] README checked
</verification>

<success_criteria>

- WORLD-01 and WORLD-02 traceable in docs

</success_criteria>
