---
phase: 02-identity-economy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - ECON-01
  - ECON-02
user_setup:
  - Running MySQL (e.g. Docker stack) for optional live SQL check
must_haves:
  truths:
    - `/money` and money mutation paths call `savePlayerMoney` which runs `UPDATE players SET money = ?, bank = ? WHERE email = ?`.
    - README describes how players see balances and that persistence is MySQL-backed.
  artifacts:
    - README **Economy** or chat section mentions `/money` and persistence.
  key_links:
    - `savePlayerMoney`, `refreshPlayerMoney`, `handleCommand` case `money` / `givemoney` in `src/plugins/gta-mysql-core/server/index.ts`
---

<objective>
Verify **cash/bank** readout and **MySQL persistence** for money changes made through implemented commands.

Purpose: Satisfy **ECON-01** and **ECON-02**.

Output: README clarification if needed; SUMMARY with SQL snippet references and manual test steps.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace money persistence code path</name>
  <files>src/plugins/gta-mysql-core/server/index.ts</files>
  <read_first>src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
1. Locate `savePlayerMoney(email, money, bank)` and quote the exact SQL in SUMMARY: `UPDATE players SET money = ?, bank = ? WHERE email = ?`.

2. List in SUMMARY at least **three** distinct code paths that call `savePlayerMoney` or otherwise persist money (e.g. `givemoney` case, disconnect handler if present, shop purchases) by naming the surrounding function or `switch` case label and line range (approximate OK).

3. Locate `refreshPlayerMoney` and note it re-reads `SELECT money, bank FROM players WHERE id = ?` into session — add one sentence to SUMMARY on when refresh is used (grep call sites).

4. If `README.md` **Money** chat section does not mention that balances persist in MySQL after disconnect, add one sentence stating that **cash and bank are stored in the `players` table** and updated on relevant commands.
  </action>
  <verify>grep -n "savePlayerMoney" src/plugins/gta-mysql-core/server/index.ts</verify>
  <acceptance_criteria>
    - `server/index.ts` contains `UPDATE players SET money = ?, bank = ? WHERE email = ?`
    - `02-03-SUMMARY.md` contains that exact SQL string in backticks or plain text
    - `README.md` contains either `players` table reference for money OR the phrase `MySQL` in the Money section context
  </acceptance_criteria>
  <done>Persistence path documented.</done>
</task>

<task type="auto">
  <name>Task 2: Manual verification checklist</name>
  <files>none (SUMMARY only)</files>
  <read_first>src/plugins/gta-mysql-core/server/index.ts, README.md</read_first>
  <action>
Append to `02-03-SUMMARY.md` a section **Manual UAT (operator)** with numbered steps:

1. Start stack (`docker compose up` per README).
2. Connect with alt:V client; open Auth UI (T); register and log in.
3. Run `/money` — note cash/bank displayed.
4. Run `/givemoney 500` (or documented debug command); run `/money` again.
5. Disconnect and reconnect; log in again; run `/money` — expect values persisted.

State clearly: **If any step cannot be run in automation, mark "Skipped in agent execution"** and leave checklist for human operator — do not claim UAT was executed unless it was.
  </action>
  <verify>grep -n "Manual UAT" .planning/phases/02-identity-economy/02-03-SUMMARY.md</verify>
  <acceptance_criteria>
    - `02-03-SUMMARY.md` contains the heading `## Manual UAT` or `## Manual UAT (operator)`
    - Same file contains the substring `/givemoney` and `/money`
  </acceptance_criteria>
  <done>Human-verifiable steps recorded.</done>
</task>

</tasks>

<verification>
- [ ] SQL and call sites documented
- [ ] Manual UAT section present
</verification>

<success_criteria>

- ECON-01/ECON-02 mapped to real code + checklist
- No false claim of in-game UAT without execution

</success_criteria>
