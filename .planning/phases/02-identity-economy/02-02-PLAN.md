---
phase: 02-identity-economy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - Passwords at rest use bcrypt with rounds from `BCRYPT_ROUNDS` (default 10).
    - Duplicate registration and wrong-password paths return the documented `AuthService` messages (grep-verifiable).
  artifacts:
    - README Security or Authentication note references bcrypt + env rounds (optional subsection under Docker/Env or new short **Security** bullet).
  key_links:
    - `AuthService.hashPassword` / `comparePassword` in `src/plugins/gta-mysql-core/server/services/AuthService.ts`
---

<objective>
Verify **password hashing and auth failure messages** in `AuthService` and surface them for operators/developers.

Purpose: Satisfy **AUTH-03** and roadmap plan **02-02** (bcrypt + failure modes).

Output: Short README note on bcrypt; execution SUMMARY with grep evidence.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/AuthService.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm bcrypt usage and rounds</name>
  <files>src/plugins/gta-mysql-core/server/services/AuthService.ts, README.md</files>
  <read_first>src/plugins/gta-mysql-core/server/services/AuthService.ts, README.md</read_first>
  <action>
1. In `AuthService.ts`, confirm `import bcrypt from 'bcryptjs'`, `BCRYPT_ROUNDS = parseInt(process.env.BCRYPT_ROUNDS || '10', 10)`, `hashPassword` uses `bcrypt.hash(plain, BCRYPT_ROUNDS)`, and `comparePassword` uses `bcrypt.compare`.

2. Add a **short** subsection to `README.md` (e.g. under **Environment Variables** after `BCRYPT_ROUNDS` or a 2–3 line **Authentication** note): state that passwords are stored as **bcrypt** hashes in `players.password_hash` and rounds are controlled by **`BCRYPT_ROUNDS`** (default 10).

Do not change hashing code in this plan unless you find a security bug (then document as deviation).
  </action>
  <verify>grep -n "bcrypt" src/plugins/gta-mysql-core/server/services/AuthService.ts</verify>
  <acceptance_criteria>
    - `AuthService.ts` contains `bcrypt.hash` and `bcrypt.compare`
    - `README.md` contains the substring `bcrypt` and the substring `BCRYPT_ROUNDS`
  </acceptance_criteria>
  <done>Bcrypt behavior documented and verified in code.</done>
</task>

<task type="auto">
  <name>Task 2: Catalog failure-mode messages</name>
  <files>src/plugins/gta-mysql-core/server/services/AuthService.ts</files>
  <read_first>src/plugins/gta-mysql-core/server/services/AuthService.ts</read_first>
  <action>
In plan **SUMMARY.md** (created at execution time), add a markdown table **Auth failure messages** with two rows minimum:

| Scenario | Exact message string returned to client (from `result.message`) |
|----------|------------------------------------------------------------------|
| Duplicate email on register | Copy exact string from `register()` when `existingEmail` — must be `This email is already registered.` |
| Wrong password on login | Copy exact string from `login()` when password does not match — must be `Invalid username/email or password.` |

Add a third row for duplicate username if present: exact string `This username is already taken.`

Values must be **verbatim** from source (copy-paste).
  </action>
  <verify>grep -F "This email is already registered." src/plugins/gta-mysql-core/server/services/AuthService.ts</verify>
  <acceptance_criteria>
    - `AuthService.ts` contains the exact line `This email is already registered.`
    - `AuthService.ts` contains the exact line `Invalid username/email or password.`
    - Plan `02-02-SUMMARY.md` contains a markdown table with header row including `Scenario` and `Exact message`
  </acceptance_criteria>
  <done>Failure strings captured for QA/regression.</done>
</task>

</tasks>

<verification>
- [ ] bcrypt paths confirmed
- [ ] SUMMARY table has verbatim strings
</verification>

<success_criteria>

- AUTH-03 evidenced end-to-end
- Operators know how rounds are configured

</success_criteria>
