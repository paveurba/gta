---
phase: 02-identity-economy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - Operators know registration and login go through the Auth UI (press T), not `/register` / `/login` chat text.
    - `playerSessions`, `player.setMeta('playerId', ...)`, and `completeLogin` are documented as the session binding path.
  artifacts:
    - `.planning/phases/02-identity-economy/02-AUTH-FLOW.md` exists with the event names below.
  key_links:
    - `alt.onClient('auth:register'` and `alt.onClient('auth:login'` in `src/plugins/gta-mysql-core/server/index.ts`
---

<objective>
Trace **registration and login** from client events through `AuthService` into `PlayerSession` and `completeLogin`, and **align README + REQUIREMENTS** with the real UX (Auth menu, not chat commands).

Purpose: Satisfy **AUTH-01**, **AUTH-02**, **AUTH-04** against actual code paths.

Output: `02-AUTH-FLOW.md`, updated `README.md` chat table, updated `.planning/REQUIREMENTS.md` wording for AUTH-01/AUTH-02.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/index.ts
@src/plugins/gta-mysql-core/server/services/AuthService.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write AUTH-FLOW reference doc</name>
  <files>.planning/phases/02-identity-economy/02-AUTH-FLOW.md</files>
  <read_first>src/plugins/gta-mysql-core/server/index.ts, src/plugins/gta-mysql-core/server/services/AuthService.ts</read_first>
  <action>
Create **new file** `.planning/phases/02-identity-economy/02-AUTH-FLOW.md` with markdown sections:

1. **Entry points:** State that `alt.onClient('auth:register', ...)` and `alt.onClient('auth:login', ...)` in `src/plugins/gta-mysql-core/server/index.ts` are the server handlers (UI sends these; chat `/register` and `/login` only show `Use the Auth menu (press T)...`).

2. **Register path:** Bullet list: `clearExistingSession` → `authService.register({ username, email, password })` → on success build `PlayerSession` with `oderId: result.session.playerId`, `email`, `money`, `bank` → `completeLogin(player, session)` → `auth:registerResult` emit.

3. **Login path:** Same style: `getMySQLPool`, `clearExistingSession`, `authService.login(loginIdentifier, password)` → session object → if `passwordChangeRequired`, partial session + client message; else `completeLogin`.

4. **Session binding:** `completeLogin` sets `playerSessions.set(player.id, session)`, `player.setMeta('playerId', session.oderId)`, `alt.emitClient(player, 'gta:playerId', session.oderId)`, then `bindCharacterForPlayer`, spawn, appearance, weapons, `syncMoneyToClient`.

5. **Logout:** Reference `auth:logout` handler and chat `logout` command clearing `playerSessions` and meta.

Use **exact string** literals for event names: `auth:register`, `auth:login`, `auth:registerResult`, `auth:loginResult`, `gta:playerId`, `gta:logout`.
  </action>
  <verify>test -f .planning/phases/02-identity-economy/02-AUTH-FLOW.md</verify>
  <acceptance_criteria>
    - File `02-AUTH-FLOW.md` contains the substring `auth:register`
    - Same file contains `auth:login` and `completeLogin`
    - Same file contains `playerSessions.set`
  </acceptance_criteria>
  <done>Auth flow doc is accurate to current server index.</done>
</task>

<task type="auto">
  <name>Task 2: Align README and REQUIREMENTS with Auth UI</name>
  <files>README.md, .planning/REQUIREMENTS.md</files>
  <read_first>README.md, .planning/REQUIREMENTS.md, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
1. In `README.md` **Chat Commands** under Authentication, **replace** rows that imply `/register` / `/login` with chat-only email/password with rows that state:
   - Open Auth UI: press **`T`** (or whatever the doc already says for chat key), then use **Register** / **Login** in the UI with **username**, **email**, and **password** (register) or **username or email** + **password** (login) — match what `AuthService` validates (`RegisterInput` has username+email+password; login uses `findByEmailOrUsername`).

2. Mention that typing `/register` or `/login` in chat shows the redirect message to the Auth menu (quote the exact message from `handleCommand` in `server/index.ts`: `Use the Auth menu (press T) to login or register.`).

3. In `.planning/REQUIREMENTS.md`, update **AUTH-01** and **AUTH-02** bullet text so they say users register/login **through the in-game Auth UI (press T to open)** — not "via chat" as the primary path. Keep them testable (same capabilities).

Do not change requirement IDs.
  </action>
  <verify>grep -n "Auth menu" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains the exact substring `Use the Auth menu (press T) to login or register.` OR documents that `/register` and `/login` show that message (paraphrase allowed if the quoted string appears in a "Behavior" note)
    - `.planning/REQUIREMENTS.md` lines for AUTH-01 and AUTH-02 each contain either `Auth UI` or `press T`
  </acceptance_criteria>
  <done>Docs match UI-based auth implementation.</done>
</task>

</tasks>

<verification>
- [ ] `02-AUTH-FLOW.md` present and cites server handlers
- [ ] README + REQUIREMENTS updated
</verification>

<success_criteria>

- AUTH-01/AUTH-02/AUTH-04 traceability documented
- No false claim that email-only `/register <email> <password>` works unless code implements it

</success_criteria>
