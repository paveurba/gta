---
phase: 14-client-modularization
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REFACTOR-CLIENT-01
user_setup: []
must_haves:
  truths:
    - `client/index.ts` is a thin entry (imports + `alt.log` only).
    - Shared client state lives in `client/state.ts` (`clientState`).
    - Domain modules under `client/`; no duplicate `alt.onServer` for the same event name.
    - `pnpm run compile:ts` passes.
  artifacts:
    - REQUIREMENTS **REFACTOR-CLIENT-01**, ROADMAP phase **14**, STATE counts, **14-01-SUMMARY.md**
---

<objective>
Modularize **`gta-mysql-core`** client code for readability and parity with server modularization, with zero intended behavior change.
</objective>

<context>
@.planning/phases/14-client-modularization/14-CONTEXT.md
</context>
