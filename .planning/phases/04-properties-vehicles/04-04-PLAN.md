---
phase: 04-properties-vehicles
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - VEH-03
user_setup: []
must_haves:
  truths:
    - `vehicle:getCatalog` / `vehicle:catalog` expose `VEHICLE_CATALOG`; purchase via `vehicle:buy` uses server `buyVehicle` with client-supplied price (document trust model).
    - `VEHICLE_DEALERSHIPS` coordinates match README dealership section where listed.
  artifacts:
    - `.planning/phases/04-properties-vehicles/04-DEALERSHIP.md` lists catalog source, buy RPC, dealership coords, `/dealership` command.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/VehicleService.ts` (`VEHICLE_CATALOG`, `VEHICLE_DEALERSHIPS`, `buyVehicle`)
    - `src/plugins/gta-mysql-core/server/index.ts` (`vehicle:getCatalog`, `vehicle:buy`)
---

<objective>
Document **dealership locations**, **vehicle catalog**, and **purchase path** (`vehicle:buy` + optional client UI); align **README** `/dealership` and coordinates with `VEHICLE_DEALERSHIPS`.

Purpose: Satisfy **VEH-03**.

Output: `04-DEALERSHIP.md`; README dealership table or bullets verified against code.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/VehicleService.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dealership and catalog reference</name>
  <files>.planning/phases/04-properties-vehicles/04-DEALERSHIP.md</files>
  <read_first>src/plugins/gta-mysql-core/server/services/VehicleService.ts, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
Create **`.planning/phases/04-properties-vehicles/04-DEALERSHIP.md`**:

1. **Catalog:** `VEHICLE_CATALOG` length / categories at high level; `vehicle:getCatalog` emits full array.

2. **Buy:** `vehicle:buy(model, modelHash, price)` → `buyVehicle` — note server trusts client `price`/`hash` today (security note for SUMMARY if unchanged).

3. **Dealerships:** Copy **exact** `name`, `x`, `y`, `z` from `VEHICLE_DEALERSHIPS` for both entries.

4. **Chat `/dealership`:** Teleport coords in `handleCommand` — confirm match one dealership or document delta.

5. **Client:** Reference where dealership menu opens (`dealershipMenuOpen`, key **E** near dealership) — file `client/index.ts` paths only in doc, no full UI spec.
  </action>
  <verify>grep -n "VEHICLE_DEALERSHIPS" src/plugins/gta-mysql-core/server/services/VehicleService.ts</verify>
  <acceptance_criteria>
    - `04-DEALERSHIP.md` contains `vehicle:buy` or `vehicle:getCatalog`
    - Same file lists both dealership names from source (`Premium` and `Simeon` substrings acceptable)
  </acceptance_criteria>
  <done>Dealership flow documented.</done>
</task>

<task type="auto">
  <name>Task 2: README dealership vs code</name>
  <files>README.md</files>
  <read_first>README.md, src/plugins/gta-mysql-core/server/services/VehicleService.ts, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
Compare README **Vehicle Dealerships** / **Map Locations** coords and names to `VEHICLE_DEALERSHIPS` and `/dealership` teleport target. Edit README if any coordinate or name is wrong.

State that buying uses the **in-world dealership menu** (native client) after teleport/nearby **E**, not a chat `/buy` with model name unless code supports it (match code).
  </action>
  <verify>grep -n "dealership" README.md</verify>
  <acceptance_criteria>
    - README mentions `/dealership` teleport or dealership interaction
    - README coordinates for **Premium Deluxe Motorsport** match `VEHICLE_DEALERSHIPS[0]` within 0.5 units on x/y/z OR README explicitly notes which entry `/dealership` uses
  </acceptance_criteria>
  <done>Operators can find dealerships.</done>
</task>

</tasks>

<verification>
- [ ] Dealership doc exists
- [ ] README coords checked
</verification>

<success_criteria>

- VEH-03 covered end-to-end in docs

</success_criteria>
