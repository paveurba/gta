---
phase: 04-properties-vehicles
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/plugins/gta-mysql-core/server/index.ts
autonomous: true
requirements:
  - VEH-01
  - VEH-02
  - VEH-04
  - VEH-05
user_setup: []
must_haves:
  truths:
    - `player_vehicles` rows created on purchase; chat `/myvehicles`, `/spawnvehicle`, `/car` mapped in `handleCommand`.
    - `vehicle:spawn` uses `VehicleService.spawnVehicle(player, vehicleId, ...)`.
  artifacts:
    - `.planning/phases/04-properties-vehicles/04-VEHICLES-CORE.md` covers persistence columns and spawn/despawn flags.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/VehicleService.ts` (`getPlayerVehicles`, `spawnVehicle`, `buyVehicle`)
    - `src/plugins/gta-mysql-core/server/index.ts` (vehicle events + chat cases)
---

<objective>
Document **owned vehicle listing, spawn by id, MySQL persistence, and debug `/car`**; fix **`vehicle:buy` post-purchase spawn** if arguments to `spawnVehicle` are wrong (must match `spawnVehicle(player, vehicleId, x, y, z, heading)`).

Purpose: Satisfy **VEH-01**, **VEH-02**, **VEH-04**, **VEH-05**.

Output: `04-VEHICLES-CORE.md`; README Vehicles commands accurate; **bugfix** in `index.ts` if buy auto-spawn passes wrong first argument.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/VehicleService.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vehicles core reference + persistence</name>
  <files>.planning/phases/04-properties-vehicles/04-VEHICLES-CORE.md</files>
  <read_first>src/plugins/gta-mysql-core/server/services/VehicleService.ts, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
Create **`.planning/phases/04-properties-vehicles/04-VEHICLES-CORE.md`**:

1. **Table:** `player_vehicles` fields used by `PlayerVehicle` interface (model, garage_property_id, is_spawned, etc.).

2. **Chat:** `/myvehicles`, `/spawnvehicle`, `/car` — exact `handleCommand` behavior and session use of `session.oderId`.

3. **RPC:** `vehicle:getMyVehicles`, `vehicle:spawn`, `vehicle:sell` summary.

4. **buyVehicle:** INSERT + `players.money` update + `transaction_logs`.

5. **spawnVehicle:** signature from source; plate text pattern `GTA{id}`.
  </action>
  <verify>grep -n "player_vehicles" src/plugins/gta-mysql-core/server/services/VehicleService.ts | head -3</verify>
  <acceptance_criteria>
    - `04-VEHICLES-CORE.md` contains `is_spawned`
    - Same file contains `/myvehicles` or `myvehicles` case description
    - Same file contains `/car` or temporary spawn description
  </acceptance_criteria>
  <done>Persistence and commands documented.</done>
</task>

<task type="auto">
  <name>Task 2: Fix vehicle:buy auto-spawn call if broken</name>
  <files>src/plugins/gta-mysql-core/server/index.ts</files>
  <read_first>src/plugins/gta-mysql-core/server/index.ts, src/plugins/gta-mysql-core/server/services/VehicleService.ts</read_first>
  <action>
Compare `vehicle:buy` handler’s call to `vehicleService.spawnVehicle` with the **actual method signature** in `VehicleService.ts`.

If the first argument is not `alt.Player` (or the argument order does not match `(player, vehicleId, x, y, z, heading)`), **fix the call** so TypeScript/runtime align: pass `player` first, then `result.vehicleId`, then coordinates, then heading in **degrees** as the service expects (read `spawnVehicle` implementation — it multiplies heading by `Math.PI/180` internally).

After edit, run `pnpm compile:ts` from repo root and ensure exit code 0.
  </action>
  <verify>grep -A3 "vehicle:buy" src/plugins/gta-mysql-core/server/index.ts | head -20</verify>
  <acceptance_criteria>
    - `server/index.ts` contains a `spawnVehicle` invocation where the first argument is the identifier `player` (the `alt.onClient` callback parameter) OR the file compiles and spawn is removed only if purchase flow intentionally does not auto-spawn (must match product intent — default: fix argument order)
    - `pnpm compile:ts` exits 0
  </acceptance_criteria>
  <done>Buy→spawn path matches service contract.</done>
</task>

</tasks>

<verification>
- [ ] Doc complete
- [ ] compile:ts passes
</verification>

<success_criteria>

- VEH-01, VEH-02, VEH-04, VEH-05 mapped to code
- No broken auto-spawn after purchase

</success_criteria>
