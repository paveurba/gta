---
phase: 04-properties-vehicles
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - PROP-05
user_setup: []
must_haves:
  truths:
    - Garage capacity uses `properties.garage_slots` (fallback in server if missing) vs `countGarageVehicles(propertyId)`.
    - `storeVehicle` / `storeNearbyVehicle` enforce `currentCount >= garageSlots` → failure message contains `Garage is full`.
  artifacts:
    - `.planning/phases/04-properties-vehicles/04-GARAGE.md` documents slot rule + events `vehicle:store`, `vehicle:storeNearby`, `vehicle:getGarageVehicles`, `vehicle:spawnFromGarage`.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/VehicleService.ts` (`storeVehicle`, `storeNearbyVehicle`, `countGarageVehicles`)
    - `src/plugins/gta-mysql-core/server/index.ts` (vehicle store / garage handlers)
---

<objective>
Document **garage slot limits** and **store/retrieve** flows for vehicles tied to owned properties.

Purpose: Satisfy **PROP-05**.

Output: `04-GARAGE.md`; README **garage** sentence referencing slot limits (table in README or link to `README.md` property table if present).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/VehicleService.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author garage reference</name>
  <files>.planning/phases/04-properties-vehicles/04-GARAGE.md</files>
  <read_first>src/plugins/gta-mysql-core/server/services/VehicleService.ts, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
Create **`.planning/phases/04-properties-vehicles/04-GARAGE.md`**:

1. **Slot rule:** Quote logic: `countGarageVehicles(propertyId) >= garageSlots` → return message including `Garage is full` and slot count (verbatim substring from `VehicleService.ts`).

2. **storeVehicle:** Updates `player_vehicles.garage_property_id`, clears spawn state, destroys spawned alt entity when applicable.

3. **storeNearbyVehicle:** 10m radius, plate prefix `GTA` + id parse → delegates to `storeVehicle`.

4. **Events:** `vehicle:store`, `vehicle:storeNearby`, `vehicle:getGarageVehicles`, `vehicle:spawnFromGarage` — which handler in `index.ts` and ownership checks (`property.owner_player_id === session.oderId`).

5. **plate format:** `GTA${dbVehicle.id}` from `spawnVehicle` (for nearby store matching).
  </action>
  <verify>grep -n "Garage is full" src/plugins/gta-mysql-core/server/services/VehicleService.ts</verify>
  <acceptance_criteria>
    - `04-GARAGE.md` contains `garage_slots` or `garageSlots`
    - Same file contains `vehicle:store` or `storeVehicle`
    - Same file contains substring `Garage is full` (copied from source)
  </acceptance_criteria>
  <done>Garage rules captured.</done>
</task>

<task type="auto">
  <name>Task 2: README garage hint</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/04-properties-vehicles/04-GARAGE.md</read_first>
  <action>
Add or extend README so operators know: garage capacity comes from **per-property** configuration (`garage_slots` / README property table); storing uses **E** property menu / garage flow. One short paragraph under Properties is enough.
  </action>
  <verify>grep -ni "garage" README.md | head -5</verify>
  <acceptance_criteria>
    - README contains `garage` (case-insensitive) in context of vehicles or properties
  </acceptance_criteria>
  <done>Garage limits visible to operators.</done>
</task>

</tasks>

<verification>
- [ ] Slot enforcement documented verbatim
- [ ] README mentions garage
</verification>

<success_criteria>

- PROP-05 evidenced with SQL + RPC path

</success_criteria>
