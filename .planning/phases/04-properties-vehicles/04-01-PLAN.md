---
phase: 04-properties-vehicles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - PROP-01
  - PROP-02
  - PROP-03
  - PROP-04
user_setup: []
must_haves:
  truths:
    - `/properties` and `/myproperties` map to `getAvailableProperties` / `getPlayerProperties` (or equivalent) with session checks where required.
    - `property:buy`, `property:sell`, `property:enter`, `property:exit` server handlers delegate to `PropertyService` with documented preconditions.
  artifacts:
    - `.planning/phases/04-properties-vehicles/04-PROPERTIES-SERVER.md` lists RPCs + service methods + key SQL.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/PropertyService.ts`
    - `src/plugins/gta-mysql-core/server/index.ts` (`property:*` block, `handleCommand` property cases)
---

<objective>
Document **property listing, purchase, sell, enter, exit** — chat commands, `property:*` client events, and `PropertyService` + `properties` table behavior.

Purpose: Satisfy **PROP-01**–**PROP-04**.

Output: `04-PROPERTIES-SERVER.md`; README **Properties** section aligned with **E** interaction + RPC names if missing.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/PropertyService.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write properties server reference</name>
  <files>.planning/phases/04-properties-vehicles/04-PROPERTIES-SERVER.md</files>
  <read_first>src/plugins/gta-mysql-core/server/services/PropertyService.ts, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
Create **`.planning/phases/04-properties-vehicles/04-PROPERTIES-SERVER.md`** with:

1. **Service methods:** `getAllProperties`, `getAvailableProperties`, `getPlayerProperties`, `getPropertyById`, `buyProperty`, `sellProperty`, `canEnterProperty` — one-line purpose each.

2. **Client events:** `property:buy`, `property:sell`, `property:enter`, `property:exit` — session guard, service calls, result events (`property:buyResult`, etc.).

3. **Chat:** `properties`, `myproperties` cases — which service methods back them.

4. **Enter/exit:** Note `playersInProperty` / meta usage if present in `index.ts`; interior teleport coordinates from `properties` row.

5. **Money:** Buy/sell updates `players.money` and `transaction_logs` via service.
  </action>
  <verify>test -f .planning/phases/04-properties-vehicles/04-PROPERTIES-SERVER.md</verify>
  <acceptance_criteria>
    - File contains `property:buy` and `buyProperty`
    - File contains `property:enter` or `enter` flow description
    - File contains `properties` table or `UPDATE properties`
  </acceptance_criteria>
  <done>Property lifecycle documented.</done>
</task>

<task type="auto">
  <name>Task 2: README Properties alignment</name>
  <files>README.md</files>
  <read_first>README.md, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
Under **Properties** in `README.md`, ensure:
- `/properties` lists **for-sale** properties; `/myproperties` lists **owned** (match server behavior).
- State that **buy/sell/enter/garage** use **E** near property when the property menu is open (match client + server flow); if the UI is native client drawing, say so (consistent with phone phase).

Do not invent features not in code.
  </action>
  <verify>grep -n "myproperties\|/properties" README.md</verify>
  <acceptance_criteria>
    - README contains both `/properties` and `/myproperties` in the Properties section or adjacent commands table
    - README mentions key **E** for property interaction OR explicitly references the property menu trigger used in `client/index.ts`
  </acceptance_criteria>
  <done>Operator docs match listing and interaction model.</done>
</task>

</tasks>

<verification>
- [ ] Reference doc exists
- [ ] README checked
</verification>

<success_criteria>

- PROP-01–PROP-04 traceable in one place

</success_criteria>
