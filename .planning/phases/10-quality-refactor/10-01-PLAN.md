---
phase: 10-quality-refactor
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - PROP-INT-01
user_setup: []
must_haves:
  truths:
    - Interior enter uses only `property:enterResult` client teleport; no `player.pos` on server for interior coords.
    - Chat `enter` uses `buildPropertyInteriorEnterPayload` like RPC; invalid interior does not set `playersInProperty`.
    - Exit paths do not set server `player.pos`; client handles exterior teleport.
  artifacts:
    - `04-PROPERTIES-SERVER.md` RPC table updated.
  key_links:
    - `PropertyService.ts` — `buildPropertyInteriorEnterPayload`, `hasConfiguredPropertyInterior`
    - `server/index.ts` — `property:enter`, `property:exit`, `enter`/`exit` chat cases
---

<objective>
Fix wrong-world teleport when entering owned properties; apply **client-authoritative** interior movement and **DRY** validation (SOLID: property rules live next to `PropertyService`).
</objective>

<context>
@.planning/phases/10-quality-refactor/10-CONTEXT.md
</context>
