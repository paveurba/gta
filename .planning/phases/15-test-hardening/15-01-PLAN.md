---
phase: 15-test-hardening
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - Add **Vitest** as **devDependency**; root **`package.json`** script **`test`** runs Vitest (e.g. **`vitest run`** for CI-friendly one-shot).
    - Add **`vitest.config`** (or **`vitest.config.ts`**) with **`include`** / **`root`** so tests live under e.g. **`src/**/*.test.ts`** or **`tests/**/*.test.ts`** — pick one convention and document in plan summary.
    - Unit tests for **`displayTagFromEmail`** covering at least — normal email with **`@`**, no **`@`**, **`@` at index 0**, empty string (match current implementation behavior).
    - **`pnpm run compile:ts`** and **`pnpm test`** both pass after changes.
  artifacts:
    - **15-01-SUMMARY.md** after execution
    - Optional — extend **REQUIREMENTS** / **STATE** when **15-01** ships (promote **TEST-01** checklist or leave backlog until milestone close)
---

<objective>
Establish **automated unit tests** for **pure** server helpers, starting with **`syncedMetaKeys.displayTagFromEmail`**, without alt:V or database.
</objective>

<context>
@.planning/phases/15-test-hardening/15-CONTEXT.md
</context>

## Implementation notes

1. Install: **`pnpm add -D vitest`** (pin version consistent with repo).
2. If Vitest needs path aliases for **`src/`**, mirror **`tsconfig`** paths minimally — prefer importing by **relative path** from test file to avoid config sprawl.
3. Do **not** import **`alt-server`** or Rebar entrypoints from test files in **15-01**.

## Verification loop

```bash
pnpm run compile:ts
pnpm test
```
