---
phase: 09-audit-server-trust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - AUD-TRUST-01
user_setup: []
must_haves:
  truths:
    - `buyVehicle` only completes purchase when `(model, modelHash)` matches exactly one `VEHICLE_CATALOG` row (same `model` string as catalog after normalization, same `hash` as catalog).
    - Debited amount and `transaction_logs` description use **server catalog** `price` and catalog `model` — client `price` is ignored for billing (may be omitted from signature later; keep RPC args for client compatibility).
    - Unknown or mismatched tuple returns `{ success: false, message: ... }` before any `INSERT` / money mutation.
  artifacts:
    - `04-DEALERSHIP.md` “Trust model” subsection states server-side validation is enforced (remove or narrow “client-supplied price” caveat).
    - `.planning/phases/04-properties-vehicles/VERIFICATION.md` notes **INT-02** closed / AUD-TRUST-01 satisfied with manual check steps.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/VehicleService.ts`
    - `src/plugins/gta-mysql-core/server/index.ts` (`vehicle:buy` — no behavior change beyond service return)
---

<objective>
Close audit **INT-02** / **AUD-TRUST-01** by making **`VehicleService.buyVehicle`** authoritative over catalog **price** and **model identity**, so crafted `vehicle:buy` RPCs cannot underpay or insert arbitrary hashes.

**No new test runner** — document a short **manual verification** checklist in **VERIFICATION.md** (e.g. valid buy at dealership, reject wrong price attempt via devtools RPC if applicable, reject garbage model).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-audit-server-trust/09-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-properties-vehicles/04-DEALERSHIP.md
@.planning/phases/04-properties-vehicles/VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog resolution helper + buyVehicle guard</name>
  <files>src/plugins/gta-mysql-core/server/services/VehicleService.ts</files>
  <read_first>VEHICLE_CATALOG entries (model + hash pattern), existing buyVehicle body</read_first>
  <action>
Add a **private** method, e.g. `resolveCatalogVehicle(model: string, modelHash: number): VehicleCatalogItem | null`, that:

1. Normalizes client `model` with `trim().toLowerCase()` (catalog uses lowercase `model` strings).
2. Finds `v` in `VEHICLE_CATALOG` where `v.model === normalizedModel` **and** `v.hash === modelHash`.
3. Returns `null` if no match (do **not** accept hash-only or model-only match — prevents wrong-row pricing).

At the start of **`buyVehicle`**, call the resolver; if `null`, return `{ success: false, message: 'Invalid vehicle or price tampering detected' }` (or similar, user-visible).

Use **`catalog.price`** for balance check, `INSERT` model string **`catalog.model`**, and **`catalog.hash`** as `model_hash` in SQL (or keep using `modelHash` param after validation — must equal catalog). Use **`catalog.price`** in `transaction_logs` description amount.

Ensure **no** `UPDATE players` or `INSERT player_vehicles` runs on validation failure.
  </action>
  <verify>pnpm run compile:ts</verify>
  <acceptance_criteria>
    - TypeScript compiles (`pnpm compile:ts` or project-equivalent)
    - `buyVehicle` uses catalog row for price and persisted model/hash only after successful resolution
  </acceptance_criteria>
  <done>Server-side catalog enforcement implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Update dealership reference doc</name>
  <files>.planning/phases/04-properties-vehicles/04-DEALERSHIP.md</files>
  <read_first>Current Purchase RPC / Trust model section</read_first>
  <action>
Replace the “client-supplied price / no re-validation” narrative with **post-fix** behavior: tuple validated against `VEHICLE_CATALOG`; **catalog price** charged; failure modes listed briefly.
  </action>
  <verify>rg -n "Trust model|buyVehicle|vehicle:buy" .planning/phases/04-properties-vehicles/04-DEALERSHIP.md</verify>
  <acceptance_criteria>
    - Doc no longer claims server trusts client price for successful purchases
  </acceptance_criteria>
  <done>DEALERSHIP doc matches code.</done>
</task>

<task type="auto">
  <name>Task 3: VERIFICATION + retro summary touch</name>
  <files>.planning/phases/04-properties-vehicles/VERIFICATION.md, .planning/phases/04-properties-vehicles/04-04-SUMMARY.md</files>
  <read_first>VEH-03 row and Notes in VERIFICATION.md; 04-04-SUMMARY deviations</read_first>
  <action>
In **VERIFICATION.md**, under Phase 4 notes or VEH-03, add **Manual checks (AUD-TRUST-01)** bullet list (3–5 lines): normal dealership buy succeeds; mismatched hash/model fails; etc.

In **04-04-SUMMARY.md**, update **Deviations** / closing note: remove or shorten “Phase 9 catalog enforcement” deferral — point to completed **09-01** instead.
  </action>
  <verify>test -f .planning/phases/04-properties-vehicles/VERIFICATION.md</verify>
  <acceptance_criteria>
    - VERIFICATION.md references catalog enforcement as satisfied post-execute
  </acceptance_criteria>
  <done>Traceability updated.</done>
</task>

</tasks>

<verification_loop>
- `pnpm compile:ts` clean.
- Grep `buyVehicle` for early `return` on catalog miss.
- Read `vehicle:buy` handler still passes session money; service returns failure without side effects when invalid.
</verification_loop>
