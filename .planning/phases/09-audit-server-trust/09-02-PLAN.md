---
phase: 09-audit-server-trust
plan: 02
type: execute
wave: 1
depends_on:
  - 09-01-PLAN.md
files_modified: []
autonomous: true
requirements:
  - AUD-TRUST-02
  - AUD-DOC-01
user_setup: []
must_haves:
  truths:
    - Before `spawnVehicle` in **`vehicle:spawnFromGarage`**: `property.owner_player_id === session.oderId` (same pattern as `vehicle:store`).
    - Load `player_vehicles` row by `vehicleId`: `player_id === session.oderId` and `garage_property_id === propertyId` and `is_spawned === false` (or equivalent checks via `VehicleService` if you add a small helper to avoid duplicating SQL rules).
    - On failure: `notifyPlayer` + **return** — no spawn, no garage list refresh.
  artifacts:
    - `04-GARAGE.md` RPC table for `vehicle:spawnFromGarage` documents ownership + garage membership checks.
    - **WEBV-01** in `.planning/milestones/v1.0-REQUIREMENTS.md` reflects native phone vs Vue webview shell (archived milestone clarification).
    - `06-WEBVIEW.md` (and README only if any sentence still contradicts) aligned with **AUD-DOC-01**.
    - `.planning/phases/04-properties-vehicles/VERIFICATION.md` notes **INT-03** / AUD-TRUST-02.
  key_links:
    - `src/plugins/gta-mysql-core/server/index.ts` (`vehicle:spawnFromGarage`, `vehicle:store`)
    - `.planning/milestones/v1.0-REQUIREMENTS.md`
---

<objective>
Close **INT-03** / **AUD-TRUST-02** on garage spawn, and **INT-01** / **AUD-DOC-01** by fixing **WEBV-01** wording in the **archived v1.0 requirements** plus any tight doc deltas.

Depends on **09-01** so garage + doc commit can ship in one milestone slice or immediately after catalog hardening (merge order: execute 09-01 then 09-02).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-audit-server-trust/09-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-properties-vehicles/04-GARAGE.md
@.planning/milestones/v1.0-REQUIREMENTS.md
@README.md
@.planning/phases/06-world-webview/06-WEBVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: spawnFromGarage guards</name>
  <files>src/plugins/gta-mysql-core/server/index.ts</files>
  <read_first>`vehicle:spawnFromGarage` block; `vehicle:store` ownership pattern; VehicleService.getVehicleById</read_first>
  <action>
After `getPropertyById` and null check:

1. If `property.owner_player_id !== session.oderId`, notify and return.
2. `const dbVehicle = await vehicleService.getVehicleById(vehicleId)` — if missing, notify and return.
3. If `dbVehicle.player_id !== session.oderId`, notify and return.
4. If `dbVehicle.garage_property_id !== propertyId`, notify (e.g. not stored in this garage) and return.
5. If `dbVehicle.is_spawned`, notify and return (already out; consistent with `spawnVehicle`).

Then proceed with existing garage coordinate resolution and `spawnVehicle`.
  </action>
  <verify>pnpm run compile:ts 2>&1 | tail -8</verify>
  <acceptance_criteria>
    - All five guards present before `spawnVehicle`
    - Messages are player-visible and distinct enough to debug (optional)
  </acceptance_criteria>
  <done>Garage spawn trust model enforced.</done>
</task>

<task type="auto">
  <name>Task 2: 04-GARAGE.md + VERIFICATION.md</name>
  <files>.planning/phases/04-properties-vehicles/04-GARAGE.md, .planning/phases/04-properties-vehicles/VERIFICATION.md</files>
  <read_first>Current `vehicle:spawnFromGarage` row in RPC table</read_first>
  <action>
Update **04-GARAGE.md** `vehicle:spawnFromGarage` row: document property owner check, vehicle owner check, `garage_property_id` match, not-already-spawned.

Append **Manual checks (AUD-TRUST-02)** to **VERIFICATION.md** (or merge with Phase 9 section): e.g. spawn from own garage OK; wrong `propertyId` fails; vehicle in another garage fails.
  </action>
  <verify>rg "spawnFromGarage" .planning/phases/04-properties-vehicles/04-GARAGE.md</verify>
  <acceptance_criteria>
    - Doc matches handler behavior
  </acceptance_criteria>
  <done>Docs updated.</done>
</task>

<task type="auto">
  <name>Task 3: WEBV-01 / INT-01 documentation</name>
  <files>.planning/milestones/v1.0-REQUIREMENTS.md, .planning/phases/06-world-webview/06-WEBVIEW.md, .planning/phases/06-world-webview/06-03-SUMMARY.md</files>
  <read_first>WEBV-01 bullet; 06-WEBVIEW Native vs Vue; 06-03-SUMMARY AUD-DOC-01 mention</read_first>
  <action>
1. **v1.0-REQUIREMENTS.md**: Replace the **WEBV-01** one-liner with accurate wording, e.g. Vue webview bundle builds and loads as the **Rebar** shell; **phone, auth, shops, property, dealership, chat** use the **native** `gta-mysql-core` client. Add a short parenthetical *(wording clarified v1.1 gap closure)* if you want archive readers to see drift from the original marketing line.

2. **06-WEBVIEW.md**: Add a single **Requirements traceability** line referencing **WEBV-01** / **INT-01** resolution.

3. **06-03-SUMMARY.md**: Update the note that deferred **INT-01** to Phase 9 — mark as addressed once doc + archive line are done (retro summary edit).
  </action>
  <verify>rg "WEBV-01" .planning/milestones/v1.0-REQUIREMENTS.md .planning/phases/06-world-webview/06-WEBVIEW.md</verify>
  <acceptance_criteria>
    - No document states phone UI is implemented in Vue webview
  </acceptance_criteria>
  <done>AUD-DOC-01 satisfied.</done>
</task>

</tasks>

<verification_loop>
- `pnpm compile:ts` clean after index.ts edits.
- Re-read `vehicle:spawnFromGarage` for ordering: property → vehicle → spawn.
- Skim README Webview section; change only if contradiction remains.
</verification_loop>
