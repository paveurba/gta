---
phase: 01-platform-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - OPS-02
user_setup: []
must_haves:
  truths:
    - Operators understand that MySQL schema comes from `database/init/` on first container start and from `runMigrations()` on every server start.
    - README lists every table that README claims exists, and those tables appear in migrations or init SQL.
  artifacts:
    - README.md schema section consistent with `database/init/001_schema.sql` and `migrations.ts` table set.
  key_links:
    - `src/plugins/gta-mysql-core/server/index.ts` calls `runMigrations` after pool creation (referenced in docs or SUMMARY).
---

<objective>
Validate the **MySQL persistence bootstrap path**: Docker init scripts plus runtime migrations — and align `README.md` **Database Schema** with the actual DDL.

Purpose: Satisfy **OPS-02** (fresh volume gets schema; migrations run on server start).

Output: README fixes if table list is wrong; execution SUMMARY documenting the two-layer strategy (init SQL vs migrations).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@database/init/001_schema.sql
@src/plugins/gta-mysql-core/server/database/migrations.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map init SQL vs migrations</name>
  <files>database/init/001_schema.sql, src/plugins/gta-mysql-core/server/database/migrations.ts, src/plugins/gta-mysql-core/server/index.ts</files>
  <read_first>database/init/001_schema.sql, src/plugins/gta-mysql-core/server/database/migrations.ts, src/plugins/gta-mysql-core/server/index.ts</read_first>
  <action>
1. Confirm `getMySQLPool()` in `src/plugins/gta-mysql-core/server/index.ts` awaits `runMigrations(mysqlPool)` after `createPool`.

2. Extract all `CREATE TABLE IF NOT EXISTS` table names from `migrations.ts` (e.g. `players`, `player_weapons`, …).

3. Extract table names from `database/init/001_schema.sql` the same way.

4. In SUMMARY, add a short subsection **Bootstrap path**: state that `./database/init` is mounted into MySQL container `docker-entrypoint-initdb.d` (from docker-compose.yml) for first-time DB, and that `runMigrations` is the authoritative incremental path when the server starts.

5. If `001_schema.sql` and `migrations.ts` disagree on column-level details for the same table, note it in SUMMARY as **Schema drift risk** — do not silently change DDL without explicit user approval; only fix README list vs obvious missing table names.
  </action>
  <verify>grep -n "runMigrations" src/plugins/gta-mysql-core/server/index.ts</verify>
  <acceptance_criteria>
    - `src/plugins/gta-mysql-core/server/index.ts` contains the substring `runMigrations`
    - SUMMARY.md (after execution) contains the literal heading `## Bootstrap path` or the phrase `runMigrations` describing server-start migrations
    - `migrations.ts` contains `CREATE TABLE IF NOT EXISTS` for `players` (grep-verifiable)
  </acceptance_criteria>
  <done>Bootstrap story documented; code path for migrations confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Align README table list with code</name>
  <files>README.md</files>
  <read_first>README.md, database/init/001_schema.sql, src/plugins/gta-mysql-core/server/database/migrations.ts</read_first>
  <action>
Compare README section **Database Schema** (bullet list of tables) to tables created in `migrations.ts`. Every table README claims must exist as `CREATE TABLE IF NOT EXISTS <name>` in migrations.ts (or be removed from README if obsolete). Add any missing tables README omits but migrations create (e.g. `character_appearance`, `casino_transactions`, `transaction_logs` — verify actual names via grep).

Use exact table names in README as in SQL (backtick or plain — match one style consistently with existing README).
  </action>
  <verify>grep "CREATE TABLE" src/plugins/gta-mysql-core/server/database/migrations.ts</verify>
  <acceptance_criteria>
    - README.md contains the substring `players` in the Database Schema section
    - For each table name listed in README Database Schema bullets, that name appears as `CREATE TABLE IF NOT EXISTS` in `migrations.ts` OR README is edited to remove/rename the bullet
  </acceptance_criteria>
  <done>README table inventory matches migration DDL names.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `runMigrations` usage confirmed in server index
- [ ] README schema bullets match migration table names
</verification>

<success_criteria>

- OPS-02 evidence documented (init + migrations)
- README schema list accurate
- Plan SUMMARY.md filed

</success_criteria>
