---
phase: 01-platform-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - OPS-01
user_setup: []
must_haves:
  truths:
    - README Docker Services table matches docker-compose.yml service names and ports (including GAME_PORT default 7788 TCP/UDP).
    - docker compose config validates without errors when run from repo root.
  artifacts:
    - README.md lists altv-server, mysql, mongodb with correct port descriptions.
  key_links:
    - docker-compose.yml environment variables for altv-server align with README Environment Variables section.
---

<objective>
Verify and align **Docker Compose** service definitions, exposed ports, and environment variables with `README.md` so operators can bring up the stack without guessing.

Purpose: Satisfy **OPS-01** (fresh compose brings up MySQL, MongoDB, alt:V on documented ports).

Output: Updated `README.md` only if drift is found; confirmation notes in phase `SUMMARY` after execution.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/INTEGRATIONS.md
@docker-compose.yml
@README.md
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-check compose vs README</name>
  <files>docker-compose.yml, README.md, .env.example</files>
  <read_first>docker-compose.yml, README.md, .env.example</read_first>
  <action>
Read `docker-compose.yml` and extract: service names (`mongodb`, `mysql`, `altv-server`), published ports (MySQL `${MYSQL_EXPOSE_PORT:-3306}`, Mongo `27017`, game `${GAME_PORT:-7788}` TCP/UDP), and env vars passed to `altv-server` (`MONGODB`, `DB_*`, `MAIL_*`, `BCRYPT_ROUNDS`).

Compare to `README.md` sections **Docker Services**, **Environment Variables**, and **Quick Start**. For every mismatch (wrong service name, wrong default port, missing env var), edit `README.md` so it matches `docker-compose.yml` and `.env.example` exactly. Do not change `docker-compose.yml` unless you find an obvious bug — this plan is documentation alignment for OPS-01.

Concrete targets to verify exist in README: port **7788** for alt:V, **3306** for MySQL (or document `MYSQL_EXPOSE_PORT`), **27017** for MongoDB; three services named consistently with the compose file.
  </action>
  <verify>grep -E '7788|3306|27017|altv-server|mysql|mongodb' README.md</verify>
  <acceptance_criteria>
    - README.md contains the substring `7788` in the Docker or connect section
    - README.md contains `mysql` and `mongodb` (or MongoDB) as service names matching docker-compose.yml intent
    - README.md Environment Variables block includes `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `MONGODB`, and `GAME_PORT` as documented in .env.example or docker-compose environment
  </acceptance_criteria>
  <done>README reflects compose ports and env; no undocumented drift for the three services.</done>
</task>

<task type="auto">
  <name>Task 2: Validate compose file syntax</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml</read_first>
  <action>
From the repository root, run: `docker compose config` (or `docker-compose config` if that is the only CLI available). If the command is unavailable, state that in SUMMARY.md under limitations and skip with reason — do not fake success.

If the command runs, it must exit 0. If it fails, capture stderr in SUMMARY and fix only YAML/syntax issues in docker-compose.yml if the error is clearly a typo (otherwise document as blocker).
  </action>
  <verify>docker compose config</verify>
  <acceptance_criteria>
    - Command `docker compose config` exits with code 0 when Docker CLI is installed, OR SUMMARY.md contains the literal phrase `Docker CLI unavailable` explaining skip
  </acceptance_criteria>
  <done>Compose file validates or skip is documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep` confirms README contains required port and service strings
- [ ] `docker compose config` exits 0 OR SUMMARY documents CLI unavailability
</verification>

<success_criteria>

- All tasks completed
- OPS-01 alignment between compose and docs is explicit
- SUMMARY.md created for this plan per execute-plan workflow

</success_criteria>
