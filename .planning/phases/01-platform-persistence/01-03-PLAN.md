---
phase: 01-platform-persistence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - OPS-03
user_setup:
  - Node.js and pnpm installed for compile verification
must_haves:
  truths:
    - Documented refresh path (`pnpm refresh` → compile + docker compose up --build) matches `package.json` scripts.
    - `pnpm compile:ts` completes successfully when dependencies are installed (or failure is captured with cause in SUMMARY).
  artifacts:
    - README.md **After making changes** / **Quick Start** steps remain consistent with actual script names in package.json.
  key_links:
    - `scripts/compile.js` is referenced in SUMMARY as the full compile entry used by `build:docker` / Docker image.
---

<objective>
Verify the **documented rebuild path** after code changes: TypeScript + webview compile scripts and `pnpm refresh` / Docker build — and fix README if script names or order are wrong.

Purpose: Satisfy **OPS-03** (after TS/webview edits, documented path produces a runnable server).

Output: README corrections; SUMMARY with actual commands run and exit codes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@package.json
@scripts/compile.js
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile README with package.json scripts</name>
  <files>README.md, package.json</files>
  <read_first>README.md, package.json</read_first>
  <action>
Compare README commands table and prose (`pnpm refresh`, `pnpm compile:ts`, `docker compose up --build`, `pnpm start`, `pnpm build:docker`) to actual `package.json` `scripts` keys.

Update README so every mentioned script exists in package.json with the exact name. Document the chain accurately: `refresh` runs `pnpm compile:ts && docker compose up --build -d` per package.json — if README says something different, fix README.

Add one sentence if missing: full Docker image build uses `node ./scripts/compile.js` with `-- docker` flag via `build:docker` / postinstall path — cite `scripts/compile.js` existence.
  </action>
  <verify>node -e "const p=require('./package.json'); console.log(Object.keys(p.scripts).join(','))"</verify>
  <acceptance_criteria>
    - README.md contains the exact substring `pnpm refresh`
    - package.json scripts.refresh value equals `pnpm compile:ts && docker compose up --build -d` (character-for-character match when read from JSON)
    - If README mentions `build:docker`, that key must exist in package.json scripts
  </acceptance_criteria>
  <done>README script names and refresh flow match package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Run TypeScript compile locally</name>
  <files>package.json, scripts/compile.js</files>
  <read_first>package.json, scripts/compile.js</read_first>
  <action>
From repo root, run `pnpm compile:ts` after ensuring dependencies are installed (`pnpm install` if node_modules missing).

Record exit code in plan SUMMARY.md. If compile fails, paste the first 30 lines of error output into SUMMARY under **Compile failure** — do not leave silent failure.

Do not run `docker compose up --build` in this task unless the user environment clearly has Docker and the user expects it; OPS-03 allows compile-only proof when Docker is heavy. If you skip Docker build, state **Docker build not executed** in SUMMARY with reason.
  </action>
  <verify>pnpm compile:ts</verify>
  <acceptance_criteria>
    - SUMMARY.md states exit code of `pnpm compile:ts` as literal `exit code 0` OR contains **Compile failure** with stderr excerpt
    - If `pnpm compile:ts` was not run, SUMMARY contains the phrase `pnpm compile:ts not run` with reason (e.g. no pnpm)
  </acceptance_criteria>
  <done>Compile attempt documented with evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] README ↔ package.json script consistency checked
- [ ] compile:ts run documented with outcome
</verification>

<success_criteria>

- OPS-03 documentation accuracy
- Honest SUMMARY of tooling execution
- Plan SUMMARY.md filed

</success_criteria>
