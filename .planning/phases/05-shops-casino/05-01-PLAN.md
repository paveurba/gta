---
phase: 05-shops-casino
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - WPN-01
  - WPN-02
user_setup: []
must_haves:
  truths:
    - `weaponshop:getCatalog` / `weaponshop:catalog` expose `WEAPON_CATALOG`; `weaponshop:buy` resolves hash via `WeaponShopService.getWeaponByHash` (unknown hash → failure).
    - `PlayerWeaponService` persists rows in `player_weapons`; weapons load on login / respawn paths that call `loadWeaponsToPlayer`; save on logout / session clear.
  artifacts:
    - `.planning/phases/05-shops-casino/05-WEAPONS-SERVER.md` lists RPCs, chat `/weapons` + `/buyweapon`, `WEAPON_SHOP_LOCATIONS`, and MySQL touch points.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/WeaponShopService.ts`
    - `src/plugins/gta-mysql-core/server/services/PlayerWeaponService.ts`
    - `src/plugins/gta-mysql-core/server/index.ts` (`weaponshop:*`, `handleCommand` weapon cases, `gta:locations:update` weapon shops)
    - `src/plugins/gta-mysql-core/client/index.ts` (weapon shop blips, **E**, `weaponshop:*`)
---

<objective>
Document **weapon catalog, Ammu-Nation locations, native shop UI + chat buy path**, and **MySQL persistence** for loadouts.

Purpose: Satisfy **WPN-01**, **WPN-02**.

Output: `05-WEAPONS-SERVER.md`; README **Weapons** / map section aligned with `WEAPON_SHOP_LOCATIONS` and interaction model (**E** at red blip / Ammu-Nation).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/WeaponShopService.ts
@src/plugins/gta-mysql-core/server/services/PlayerWeaponService.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weapons server reference</name>
  <files>.planning/phases/05-shops-casino/05-WEAPONS-SERVER.md</files>
  <read_first>WeaponShopService.ts, PlayerWeaponService.ts, server/index.ts (weapon blocks)</read_first>
  <action>
Create **`.planning/phases/05-shops-casino/05-WEAPONS-SERVER.md`**:

1. **Catalog:** `WEAPON_CATALOG` categories; `weaponshop:getCatalog` → `weaponshop:catalog`.

2. **Buy:** `weaponshop:buy(weaponHash)` and chat `/buyweapon <name>` → `buyWeapon` — catalog lookup, money deduct, `giveWeapon`, `saveWeapon` / armor branch.

3. **Ammo:** `weaponshop:buyAmmo` if present — `PlayerWeaponService` / SQL behavior.

4. **Persistence:** `player_weapons` schema usage (`ON DUPLICATE KEY UPDATE` for ammo); `loadWeaponsToPlayer` vs `savePlayerWeapons` (when each runs — login, logout, death/respawn if applicable).

5. **Locations:** Full list of `WEAPON_SHOP_LOCATIONS` (name + x,y,z) matching source; note server pushes them via `gta:locations:update`.

6. **Security note:** Purchases are only for hashes present in `WEAPON_CATALOG` (contrast with vehicle buy if relevant).
  </action>
  <verify>test -f .planning/phases/05-shops-casino/05-WEAPONS-SERVER.md</verify>
  <acceptance_criteria>
    - File contains `weaponshop:buy` and `player_weapons`
    - File contains `WEAPON_SHOP_LOCATIONS` or at least one Ammu-Nation coordinate triple
    - File mentions `loadWeaponsToPlayer` or login weapon restore
  </acceptance_criteria>
  <done>Weapon flows documented.</done>
</task>

<task type="auto">
  <name>Task 2: README weapons alignment</name>
  <files>README.md</files>
  <read_first>README.md, WeaponShopService.ts, client/index.ts (weapon shop / E)</read_first>
  <action>
Update README so operators know:

- `/weapons` and `/buyweapon` behavior matches `handleCommand`.
- In-world purchase uses **native client** shop menu at Ammu-Nation blips (**E**), same pattern as properties/phone docs.
- Optional: add or verify a concise **Ammu-Nation coordinates** list vs `WEAPON_SHOP_LOCATIONS` (all 6 entries or a pointer to the planning doc if table too long).

Do not invent commands not implemented.
  </action>
  <verify>grep -n "buyweapon\|Ammu\|weapon" README.md | head -15</verify>
  <acceptance_criteria>
    - README mentions `/buyweapon` and **E** (or explicit “native weapon shop menu”) for catalog buys
    - README does not claim webview owns weapon UI
  </acceptance_criteria>
  <done>README matches code.</done>
</task>

</tasks>

<verification>
- [ ] Reference doc exists
- [ ] README updated or verified sufficient
</verification>

<success_criteria>

- WPN-01 and WPN-02 traceable in one artifact + README

</success_criteria>
