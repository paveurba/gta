---
phase: 05-shops-casino
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - CLTH-01
  - CLTH-02
user_setup: []
must_haves:
  truths:
    - `clothingshop:getCatalog` / `clothingshop:catalog` expose `CLOTHING_CATALOG`; `clothingshop:buy(component, drawable, texture)` resolves catalog row (fallback price behavior in `buyClothing` must be quoted verbatim if present).
    - `player_clothes` upsert on buy; `loadPlayerClothing` applied after appearance on login paths documented in `index.ts`.
  artifacts:
    - `.planning/phases/05-shops-casino/05-CLOTHING-SERVER.md` lists RPCs, `CLOTHING_SHOP_LOCATIONS`, `CLOTHING_COMPONENTS`, and SQL.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/ClothingShopService.ts`
    - `src/plugins/gta-mysql-core/server/index.ts` (`clothingshop:*`, `completeLogin` / appearance flow)
    - `src/plugins/gta-mysql-core/client/index.ts` (clothing blips, **E**, preview/buy)
---

<objective>
Document **clothing catalog, shop locations, preview/buy RPCs**, and **`player_clothes` persistence**.

Purpose: Satisfy **CLTH-01**, **CLTH-02**.

Output: `05-CLOTHING-SERVER.md`; README clothing / map bullets vs `CLOTHING_SHOP_LOCATIONS` (names and coords where listed).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/ClothingShopService.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clothing server reference</name>
  <files>.planning/phases/05-shops-casino/05-CLOTHING-SERVER.md</files>
  <read_first>ClothingShopService.ts, server/index.ts (clothing block)</read_first>
  <action>
Create **`.planning/phases/05-shops-casino/05-CLOTHING-SERVER.md`**:

1. **Catalog:** `CLOTHING_CATALOG` structure (`component`, `drawable`, `texture`, `name`, `price`); component ID reference from `CLOTHING_COMPONENTS`.

2. **RPCs:** `clothingshop:getCatalog`, `clothingshop:preview`, `clothingshop:buy` — session requirements.

3. **Buy logic:** How `buyClothing` finds price (exact catalog match vs default); money update; `saveClothing` upsert.

4. **Load:** `getPlayerClothing` / `loadPlayerClothing` — when called (e.g. after `loadOrCreateDefaultAppearance`).

5. **Locations:** All `CLOTHING_SHOP_LOCATIONS` entries from source.

6. **Client:** Note native menu + **E** near blue clothing blips (`client/index.ts`).
  </action>
  <verify>grep -n "clothingshop" src/plugins/gta-mysql-core/server/index.ts</verify>
  <acceptance_criteria>
    - File contains `player_clothes`
    - File contains `clothingshop:buy` or `buyClothing`
    - File lists at least three shop coordinates or names from `CLOTHING_SHOP_LOCATIONS`
  </acceptance_criteria>
  <done>Clothing stack documented.</done>
</task>

<task type="auto">
  <name>Task 2: README clothing alignment</name>
  <files>README.md</files>
  <read_first>README.md map section, ClothingShopService.ts</read_first>
  <action>
Ensure README describes clothing purchase as **in-world native UI** (**E** at clothing shops), not Vue webview-only.

Cross-check named shops in README **Map Locations** (Suburban, Ponsonbys, Binco, Discount Store) against `CLOTHING_SHOP_LOCATIONS` count and representative coords; fix any mismatched coordinates if README lists numeric positions.

If README only lists names without coords, add one line pointing to `CLOTHING_SHOP_LOCATIONS` in code or to `05-CLOTHING-SERVER.md` after it exists.
  </action>
  <verify>grep -ni "cloth\|Suburban\|Ponsonbys\|Binco" README.md | head -12</verify>
  <acceptance_criteria>
    - README mentions clothing shops and **E** or native client clothing menu
  </acceptance_criteria>
  <done>Operators understand clothing flow.</done>
</task>

</tasks>

<verification>
- [ ] Doc exists
- [ ] README checked
</verification>

<success_criteria>

- CLTH-01 and CLTH-02 mapped to code + docs

</success_criteria>
