---
phase: 05-shops-casino
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - CASI-01
  - CASI-02
user_setup: []
must_haves:
  truths:
    - `CasinoService.validateBet` enforces min/max and balance; `playSlots` / `playRoulette` update `players.money` and write `casino_transactions` + `transaction_logs`.
    - Chat `/slots` and `/roulette` delegate to the same service methods as `casino:playSlots` / `casino:playRoulette`.
  artifacts:
    - `.planning/phases/05-shops-casino/05-CASINO-SERVER.md` documents bet rules, game types, result payloads, and DB writes.
  key_links:
    - `src/plugins/gta-mysql-core/server/services/CasinoService.ts`
    - `src/plugins/gta-mysql-core/server/index.ts` (casino RPCs, `handleCommand` slots/roulette)
    - `src/plugins/gta-mysql-core/client/index.ts` (casino UI / `casino:*` result display if any)
---

<objective>
Document **casino bet validation**, **slots and roulette outcomes**, **money persistence**, and **history tables**; align README command usage with `handleCommand`.

Purpose: Satisfy **CASI-01**, **CASI-02**.

Output: `05-CASINO-SERVER.md`; README **Casino** section with min/max bet, `/roulette` argument shape, and `/casino` teleport vs `CASINO_LOCATIONS`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/plugins/gta-mysql-core/server/services/CasinoService.ts
@src/plugins/gta-mysql-core/server/index.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Casino server reference</name>
  <files>.planning/phases/05-shops-casino/05-CASINO-SERVER.md</files>
  <read_first>CasinoService.ts, server/index.ts (casino + command cases)</read_first>
  <action>
Create **`.planning/phases/05-shops-casino/05-CASINO-SERVER.md`**:

1. **Locations:** `CASINO_LOCATIONS` (Diamond Casino coords).

2. **Validation:** `MIN_BET`, `MAX_BET`, balance check — quote values from source.

3. **Slots:** Random symbol generation; how win is determined (`SLOT_PAYOUTS` / multiplier); `newBalance` formula; `logCasinoTransaction` fields.

4. **Roulette:** Supported `betType` values (`number`, `color`, `odd`, `even`); how `betValue` is interpreted vs spin result; multipliers summary.

5. **Persistence:** `casino_transactions` columns; `transaction_logs` row for `CASINO_*` types.

6. **RPC vs chat:** `casino:playSlots`, `casino:playRoulette`, `casino:getHistory` / `casino:history`; mirror chat commands.

7. **Optional verification task:** If slot payout keys use emoji triples, confirm `symbols.join('')` matches `SLOT_PAYOUTS` key format in source; if payouts never trigger, file a fix in execution (document finding in SUMMARY).
  </action>
  <verify>grep -n "playSlots\|playRoulette" src/plugins/gta-mysql-core/server/services/CasinoService.ts</verify>
  <acceptance_criteria>
    - File contains `casino_transactions`
    - File contains `/slots` or `playSlots`
    - File contains `roulette` or `playRoulette`
    - File states min and max bet numerically
  </acceptance_criteria>
  <done>Casino behavior documented.</done>
</task>

<task type="auto">
  <name>Task 2: README casino alignment</name>
  <files>README.md</files>
  <read_first>README.md, handleCommand slots/roulette cases</read_first>
  <action>
Update README **Casino** table/paragraph so:

- `/slots <bet>` default bet if omitted matches code.
- `/roulette <bet> <type> <value>` documents valid types and example values (e.g. `color red`, `number 7`) per `CasinoService` switch.
- States min/max bet ($100–$100,000 unless code differs).
- Clarifies `/casino` teleport target matches `CASINO_LOCATIONS[0]`.

Mention that in-world casino games may also use client UI + RPCs where implemented.
  </action>
  <verify>grep -n "slots\|roulette\|Casino" README.md</verify>
  <acceptance_criteria>
    - README includes min/max bet or points to doc
    - README `/roulette` line reflects `betType` options from code
  </acceptance_criteria>
  <done>README matches casino commands.</done>
</task>

</tasks>

<verification>
- [ ] Casino doc exists
- [ ] README roulette/slots accurate
</verification>

<success_criteria>

- CASI-01 and CASI-02 fully traceable

</success_criteria>
