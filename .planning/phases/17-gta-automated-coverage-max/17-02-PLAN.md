---
phase: 17-gta-automated-coverage-max
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01-PLAN.md
autonomous: true
requirements:
  - TEST-COVERAGE-17
user_setup: []
must_haves:
  truths:
    - **PropertyService** `buyProperty` / `sellProperty` core branches covered with **mocked** pool (not found, wrong owner, insufficient funds, success where practical).
    - Optional **`@vitest/coverage-v8`** + **`pnpm test:coverage`** script with `include` scoped to `src/plugins/gta-mysql-core/**` (avoid scanning entire Rebar `src/main`).
    - Update **`.planning/codebase/TESTING.md`** with Phase 17 summary and automatable vs alt:V gap.
    - **`17-01-SUMMARY.md`** and **`17-02-SUMMARY.md`** committed.
  artifacts:
    - `tests/unit/gta/propertyService.transactions.test.ts` (or split files)
    - `vitest.config.ts` coverage block (if dependency added)
---

<objective>
**Wave 2:** Property transaction service tests + optional coverage reporting + documentation + phase summaries.
</objective>

<context>
@.planning/phases/17-gta-automated-coverage-max/17-CONTEXT.md
</context>

## Tasks

1. Mock `PropertyService.getPropertyById` behavior via `pool.execute` return shapes for `buyProperty` / `sellProperty`.
2. Add `@vitest/coverage-v8` and `test:coverage` script; configure `coverage.include` for gta-mysql-core only.
3. Refresh `.planning/codebase/TESTING.md` (automated vs manual).
4. Write `17-01-SUMMARY.md`, `17-02-SUMMARY.md`.

## Verification

```bash
npx tsc --noEmit
pnpm test
pnpm test:coverage
```
