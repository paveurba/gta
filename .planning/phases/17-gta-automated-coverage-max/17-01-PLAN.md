---
phase: 17-gta-automated-coverage-max
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - TEST-01
  - TEST-COVERAGE-17
user_setup: []
must_haves:
  truths:
    - Extract or reuse **pure** auth validation (`isValidEmail`, `isValidUsername`) in a module testable without `AuthService` DB calls.
    - **Vitest** tests for **`AuthService.register`** / **`login`** validation and at least one success path each using **mocked** `pool.execute`.
    - **Vitest** tests for **`hasConfiguredPropertyInterior`** and **`buildPropertyInteriorEnterPayload`** (`PropertyService` exports).
    - **`pnpm test`** and **`npx tsc --noEmit`** pass.
  artifacts:
    - `tests/unit/gta/authValidation.test.ts` (or equivalent)
    - `tests/unit/gta/authService.test.ts`
    - `tests/unit/gta/propertyInterior.test.ts`
---

<objective>
**Wave 1:** Auth validation extraction + **AuthService** unit tests with mocks + **property interior** pure helper tests.
</objective>

<context>
@.planning/phases/17-gta-automated-coverage-max/17-CONTEXT.md
</context>

## Tasks

1. Add `server/auth/authValidation.ts` with exported `isValidEmail`, `isValidUsername` (move logic from `AuthService.ts`).
2. Update `AuthService.ts` to import validators; remove duplicate private implementations.
3. Add `authValidation.test.ts` covering edge cases for email/username rules.
4. `vi.mock('alt-server')` in `authService.test.ts`; mock `pool.execute` for register (validation-only failures without execute; success path with insert + select).
5. Add `propertyInterior.test.ts` for `hasConfiguredPropertyInterior` / `buildPropertyInteriorEnterPayload`.

## Verification

```bash
npx tsc --noEmit
pnpm test
```
