---
phase: 12-ci-hardening
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - TEST-02
user_setup: []
must_haves:
  truths:
    - New workflow **`.github/workflows/ci.yml`** runs on **`push`** and **`pull_request`** to default branch(es) (e.g. **`main`**).
    - Job uses **Node** + **pnpm** (cache optional), **`pnpm install`** / **`pnpm install --frozen-lockfile`** if lockfile present, then **`pnpm run compile:ts`** — must exit **0** on clean tree.
    - No secrets required for compile-only job.
  artifacts:
    - **[REQUIREMENTS.md](../../REQUIREMENTS.md):** Promote **TEST-02** from *v2 backlog (reference)* into **v1.2** table + checklist + traceability row (**Phase 12**, plan **12-01**, status planned → complete after execute).
    - **[ROADMAP.md](../../ROADMAP.md):** Phase **12** plan **12-01** checkbox set when shipped.
    - **[STATE.md](../../STATE.md):** Bump **`total_plans`** / **`completed_plans`** when **12-01** executes (or leave unchanged until execute — prefer update on execute only).
  key_links:
    - `package.json` — `compile:ts`
    - `.github/workflows/retype-action.yml` — prior art
---

<objective>
Ship **TEST-02**: **GitHub Actions** runs **`pnpm run compile:ts`** on every push/PR so TypeScript compile breaks fail CI before merge.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-ci-hardening/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ci.yml workflow</name>
  <files>.github/workflows/ci.yml (new)</files>
  <read_first>package.json scripts; existing retype workflow</read_first>
  <action>
1. Add **`ci.yml`** with **`on: push`**, **`pull_request`** targeting **`main`** (and **`master`** if repo uses it — prefer **`main` only** unless grep shows otherwise).

2. Steps: **`actions/checkout@v4`**, **`pnpm/action-setup`** with **`version: 9`** (or match repo), **`actions/setup-node@v4`** with **`cache: pnpm`**, **`pnpm install --frozen-lockfile`** when **`pnpm-lock.yaml`** exists else **`pnpm install`**.

3. Run **`pnpm run compile:ts`**.

4. Document in workflow comment that **`postinstall`** may run **`altv-pkg`** / **`build:docker`** — if CI fails, switch to **`pnpm install --ignore-scripts`** then run **`pnpm run compile:ts`** only if compile does not need postinstall artifacts; **verify** which path works on clean Ubuntu.
  </action>
  <verify>Push to a branch or `act` locally if available; workflow YAML valid</verify>
  <acceptance_criteria>
    - Green run on **`main`** after merge
  </acceptance_criteria>
  <done>Workflow merged.</done>
</task>

<task type="auto">
  <name>Task 2: Requirements + roadmap + summary</name>
  <files>.planning/REQUIREMENTS.md, ROADMAP.md, STATE.md, 12-01-SUMMARY.md (new)</files>
  <read_first>TEST-02 row in REQUIREMENTS v2 backlog</read_first>
  <action>
1. Move **TEST-02** into **v1.2** section with traceability **Phase 12** / **12-01**.

2. **ROADMAP** — Phase **12** section: mark **12-01** **[x]** when done.

3. **`12-01-SUMMARY.md`** — outcomes, any **`postinstall`** caveat.
  </action>
  <verify>IDs consistent with **TEST-02**</verify>
  <done>GSD traceability closed for **12-01**.</done>
</task>

</tasks>

<verification_loop>
- Open PR with intentional TS syntax error in **`src/`** — CI should **fail**.
- Revert — CI **passes**.
</verification_loop>
